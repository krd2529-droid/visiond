# PATCH v0.14.156 — Production Event Gate

- ปรับหน้าตรวจ Integrity ให้แยก “ข้อมูลตรงกัน” ออกจาก “Event Case ผ่านจริง” เพื่อไม่ให้ค่า 0=0=0 ปิดเคสผิด
- ตะกร้า `user1`/บัญชีทดสอบจะผ่านเมื่อมี paid order อย่างน้อย 1, มีหลักฐานสลิป, มีสิทธิ์เรียนเท่ากัน และคอร์ส approved/published
- หน้าและ API นี้ใช้ `GET` สำหรับ Boss/Admin เท่านั้น ไม่มีคำสั่งแก้ ลบ รีเซ็ต หรือซ่อมฐานข้อมูลอัตโนมัติ
- ติดตั้งแบบวาง ZIP ทับ ไม่เปลี่ยน Cloudflare

EVENT CASE: ยังไม่เสร็จ — ต้องแพตต่อ

คงเหลือ: ลง Production แล้วเปิด หลังบ้าน → ตรวจ Integrity; เมื่อกล่อง user1 แสดง “Event Case ผ่านจริง” จึงปิดเคสได้
