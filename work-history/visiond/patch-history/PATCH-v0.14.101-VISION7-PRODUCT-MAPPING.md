# VisionD v0.14.101 — Vision 7 Product and Package Mapping

- Product ID fields now use real product choices with ID, name, slug, price and sale status.
- Main display product is explicitly separated from lifetime, monthly and yearly sale baskets.
- Empty basket mappings retain all three plans for manual test-key issuance.
- Client and server reject duplicate, missing, malformed or already-bound product mappings.
