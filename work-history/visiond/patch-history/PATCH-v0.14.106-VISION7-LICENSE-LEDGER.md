# Patch v0.14.106 — Vision 7 Complete License Ledger

- Displays every issued key with customer ID/name, masked key, app, plan duration, cost, issue date, issuer and status.
- Preserves renew, suspend/reactivate and history actions.
- Stores issuance type and issue-cost snapshot for new keys.
- Treats test keys as zero cost and safely displays missing legacy/test data.
- Removed the prior 500-row cap.
