import {ensureVEasyShopSchema} from '../../../_veasy_shop.js';
export async function onRequestGet(ctx){await ensureVEasyShopSchema(ctx.env);const row=await ctx.env.DB.prepare("SELECT cover_image_key key FROM veasy_products WHERE id=? AND status='active'").bind(ctx.params.productId).first();if(!row?.key||!ctx.env.FILES)return new Response('not found',{status:404});const object=await ctx.env.FILES.get(row.key);if(!object)return new Response('not found',{status:404});return new Response(object.body,{headers:{'content-type':object.httpMetadata?.contentType||'image/jpeg','cache-control':'public, max-age=86400'}})}
