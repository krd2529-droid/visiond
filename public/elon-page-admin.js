const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function setStatus(id,status){await fetch('/api/admin/elon-page',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id,status})});load()}async function load(){const r=await fetch('/api/admin/elon-page'),d=await r.json();if(r.status===401||r.status===403){location.href='/login.html?next=/elon-page-admin.html';return}const s=d.stats||{};document.querySelector('#stats').innerHTML=[['ทั้งหมด',s.total],['บอทกำลังขาย',s.bot_active],['รอคนรับช่วง',s.human_required],['คนกำลังดูแล',s.human_active]].map(x=>`<article class="card"><span class="muted">${x[0]}</span><div class="metric">${Number(x[1]||0)}</div></article>`).join('');document.querySelector('#rows').innerHTML=(d.items||[]).map(x=>`<tr><td><b>${esc(x.display_name)}</b><br>${esc(x.source_campaign)}</td><td>${esc(x.last_message||'—')}<br><small>${x.message_count} ข้อความ</small></td><td><select data-id="${esc(x.id)}"><option value="bot_active">บอทขาย</option><option value="human_required">รอคนรับช่วง</option><option value="human_active">คนรับช่วง</option><option value="closed">ปิด</option></select></td></tr>`).join('')||'<tr><td colspan="3">ยังไม่มีแชทจากเพจ</td></tr>';document.querySelectorAll('[data-id]').forEach(x=>{x.value=(d.items.find(i=>i.id===x.dataset.id)||{}).status;x.onchange=()=>setStatus(x.dataset.id,x.value)})}load();
