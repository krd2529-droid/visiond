import assert from 'node:assert/strict';import fs from 'node:fs';const read=file=>fs.readFileSync(file,'utf8'),j=read('public/veasy/login.js'),h=read('public/veasy/login.html');
assert.ok(Number(read('VERSION.txt').trim().split('.').pop())>=73);assert.match(h,/name="shop_name"/);assert.match(j,/\/api\/vision7\/auth\/veasy-activate/);assert.match(j,/type:'veasy-auth-v1'/);assert.match(j,/accessToken/);assert.match(j,/accountScope/);assert.match(j,/returnState/);assert.match(j,/window\.name=''/);assert.match(read('functions/api/vision7/shops/bind.js'),/ownedShopByLicense/);console.log('v0.14.73 compatibility passed through hosted activation');
