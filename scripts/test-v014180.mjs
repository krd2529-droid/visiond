import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const r=p=>readFile(new URL('../'+p,import.meta.url),'utf8');const[version,runtime,index,ledger,active]=await Promise.all([r('VERSION.txt'),r('public/build-version.js'),r('public/index.html'),r('patch-ledgers/v0.14.180.json'),r('ACTIVE-PRODUCTION-TREE.md')]);assert.equal(version.trim(),'v0.14.180');assert.match(runtime,/v0\.14\.180/);assert.match(index,/v0\.14\.180/);assert.equal(JSON.parse(ledger).patch,'v0.14.180');assert.match(active,/Cloudflare Root directory: `visiond-mvp`/);console.log('v0.14.180 Cloudflare production source sync: PASS');
