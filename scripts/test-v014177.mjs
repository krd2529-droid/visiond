import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const r=p=>readFile(new URL('../'+p,import.meta.url),'utf8');const[orders,adminApi,adminUi,sellerUi,courseUi,html,schema,ledger]=await Promise.all([r('functions/_orders.js'),r('functions/api/admin/course-partner-payouts.js'),r('public/partner-payout-admin.js'),r('public/course-partner-balance.js'),r('public/course-seller.js'),r('public/course-seller.html'),r('functions/_schema.js'),r('patch-ledgers/v0.14.177.json')]);assert.match(orders,/visiond=Math\.floor\(gross\*/);assert.match(orders,/sellerBeforeFee=gross-visiond/);assert.match(orders,/seller=sellerBeforeFee-fee/);assert.doesNotMatch(orders,/visiond=Math\.floor\(net\*/);assert.match(adminApi,/payout_batch_no/);assert.match(adminApi,/หนึ่งรอบจ่ายต้องเป็นผู้สอนคนเดียวกัน/);assert.match(schema,/payout_batch_no/);assert.match(adminUi,/บริษัทรับรวม/);assert.match(adminUi,/หัก VisionD API จากผู้สอน/);assert.match(sellerUi,/แบ่งยอดขาย VisionD 50% \/ ผู้สอน 50% ก่อน/);assert.match(courseUi,/แบ่งยอด 50\/50 แล้วหักค่า VisionD API 1 บาทจากส่วนผู้สอน/);assert.match(html,/vision5-three-plans-v014177\.png/);assert.equal(JSON.parse(ledger).patch,'v0.14.177');const gross=10000,visiond=Math.floor(gross*50/100),sellerBeforeFee=gross-visiond,fee=100,seller=sellerBeforeFee-fee;assert.deepEqual({visiondWithApi:visiond+fee,seller},{visiondWithApi:5100,seller:4900});console.log('v0.14.177 Vision5 gross 50/50 then seller API fee: PASS');
