import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(p,'utf8'),line=r('functions/api/vision7/shops/[shopId]/line-connection.js');assert.equal(r('VERSION.txt').trim(),'v0.14.166');for(const contract of[/credentials_saved:saved/,/credential_input_required:!saved/,/credential_display:saved\?'••••••••':''/])assert.match(line,contract);assert.doesNotMatch(line,/channel_secret\s*:/);assert.doesNotMatch(line,/access_token\s*:/);assert.equal(line,r('visiond-mvp/functions/api/vision7/shops/[shopId]/line-connection.js'));console.log('PASS v0.14.166 persistent mobile settings contract');
