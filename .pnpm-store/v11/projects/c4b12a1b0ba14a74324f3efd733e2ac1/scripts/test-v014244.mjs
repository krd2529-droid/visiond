import assert from'node:assert/strict';import fs from'node:fs';const read=path=>fs.readFileSync(path,'utf8'),version=read('VERSION.txt'),page=read('public/v12-settings.html'),ui=read('public/v12-settings.js');assert.equal(version.trim(),'v0.14.244');assert.doesNotMatch(ui,/fetch\(['"]\/VERSION\.txt/);assert.match(ui,/loadSettings\(\);/);assert.doesNotMatch(ui,/label\.textContent=version/);assert.match(page,/id="v12Settings"/);console.log('v0.14.244 V12 settings production render fix: PASS');
