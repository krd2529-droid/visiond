import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const r=p=>readFile(p,'utf8');assert.equal((await r('VERSION.txt')).trim(),'v0.14.158');const a=await r('functions/api/admin/course-integrity.js'),u=await r('public/course-integrity.js');assert.match(a,/async function sampleEventCase/);assert.match(a,/LIMIT 20/);assert.match(a,/searchParams\.get\('event_case'\)===\'1\'/);assert.match(a,/requireAdmin/);assert.match(u,/course-integrity\?event_case=1/);console.log('v0.14.158 focused user1 gate PASS');
