import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const[v,i,a,api]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('functions/api/admin/v12-connect.js')]);assert.equal(v.trim(),'v0.14.202');assert.match(i,/WEB v0\.14\.202/);assert.match(a,/ADMIN v0\.14\.202/);for(const x of['message_attachments','filedata','attachment_id','external_account_id','FACEBOOK_UPLOAD_HTTP'])assert.ok(api.includes(x),x);console.log('v0.14.202 Facebook direct image upload: PASS');
