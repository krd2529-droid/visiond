import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const [version,index,admin,api,roadmap]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('functions/api/admin/v12-channel.js'),read('VISIOND-ROADMAP.md')]);assert.equal(version.trim(),'v0.14.197');assert.match(index,/WEB v0\.14\.197/);assert.match(admin,/ADMIN v0\.14\.197/);assert.match(api,/\/me\/messenger_profile\?fields=get_started/);assert.match(api,/messenger_profile_ready:true/);assert.doesNotMatch(api,/\$\{pageId\}\?fields=id,name/);assert.match(roadmap,/v0\.14\.197[\s\S]*Messenger-Scope/);console.log('v0.14.197 Messenger-scope connection test: PASS');
