import assert from'node:assert/strict';import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),v=r('VERSION.txt'),p=r('public/v12-connect.html'),u=r('public/v12-connect.js'),a=r('functions/api/admin/v12-facebook-history.js');assert.equal(v.trim(),'v0.14.238');assert.match(p,/v0\.14\.238/);for(const x of['v12-connect.css','v12-profile.css','v12-connect.js'])assert.match(p,new RegExp(x.replace('.','\\.')+'\\?v=014238'));for(const x of['v12FacebookHistory','นำเข้าแชตเก่า Facebook','/api/admin/v12-facebook-history'])assert.ok(u.includes(x),x);assert.doesNotMatch(u,/if\(active\)await openThread\(active\.shop_id,active\.id\)/);for(const x of['requireAdmin','decryptChannelValue','encryptChannelValue','INSERT OR IGNORE INTO veasy_chat_messages','next_cursor','has_more'])assert.ok(a.includes(x),x);console.log('v0.14.238 Meta history import and stable V12 thread refresh: PASS');
