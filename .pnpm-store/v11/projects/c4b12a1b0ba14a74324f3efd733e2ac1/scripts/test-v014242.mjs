import assert from'node:assert/strict';import fs from'node:fs';const read=path=>fs.readFileSync(path,'utf8'),version=read('VERSION.txt'),helper=read('functions/_facebook_profile.js'),history=read('functions/api/admin/v12-facebook-history.js'),profiles=read('functions/api/admin/v12-profiles.js'),hook=read('functions/hooks/v12/facebook.js');assert.equal(version.trim(),'v0.14.242');for(const token of['first_name,last_name,name,profile_pic','/picture?redirect=false&type=large','is_silhouette','authorization:`Bearer ${token}`'])assert.ok(helper.includes(token),token);for(const source of[history,profiles,hook])assert.match(source,/fetchFacebookProfile/);assert.doesNotMatch(helper,/access_token=/);console.log('v0.14.242 Facebook profile picture fallback: PASS');
