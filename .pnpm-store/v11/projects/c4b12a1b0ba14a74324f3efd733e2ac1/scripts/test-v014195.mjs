import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const [version,index,admin,page,ui,api,roadmap]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('public/v12-connect.html'),read('public/v12-connect.js'),read('functions/api/admin/v12-channel.js'),read('VISIOND-ROADMAP.md')]);assert.equal(version.trim(),'v0.14.195');assert.match(index,/WEB v0\.14\.195/);assert.match(admin,/ADMIN v0\.14\.195/);assert.match(page,/v0\.14\.195/);assert.match(ui,/HTTP \$\{response\.status\}/);for(const token of['graph.facebook.com/${pageId}','FACEBOOK_GRAPH_','meta_error','error_subcode','Meta ${code','Meta หา Page นี้ไม่ได้'])assert.ok(api.includes(token),token);assert.doesNotMatch(api,/return json\(\{[^}]*token/);assert.match(roadmap,/v0\.14\.195[\s\S]*Meta Direct Page Diagnostics/);console.log('v0.14.195 Meta direct diagnostics: PASS');
