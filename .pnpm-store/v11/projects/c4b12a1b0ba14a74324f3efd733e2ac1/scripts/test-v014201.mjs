import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const[v,i,a,api,ui]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('functions/api/admin/v12-connect.js'),read('public/v12-connect.js')]);assert.equal(v.trim(),'v0.14.201');assert.match(i,/WEB v0\.14\.201/);assert.match(a,/ADMIN v0\.14\.201/);assert.match(api,/providerError/);assert.match(api,/detail:clean\(error/);assert.match(ui,/data\.detail/);console.log('v0.14.201 provider diagnostics: PASS');
