import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=path=>readFile(new URL(`../${path}`,import.meta.url),'utf8');const[version,index,admin,v12,catalog,api,roadmap]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('public/v12-connect.html'),read('public/catalog-sync.js'),read('functions/api/products/index.js'),read('VISIOND-ROADMAP.md')]);
assert.equal(version.trim(),'v0.14.210');assert.match(index,/WEB v0\.14\.210/);assert.match(admin,/ADMIN v0\.14\.210/);assert.match(v12,/v0\.14\.210/);assert.match(index,/id="homeBundleDeals"[\s\S]*id="homeBundleDealsGrid"[\s\S]*สินค้าดิจิทัลแนะนำ/);assert.match(index,/catalog-sync\.js\?v=014210/);
for(const token of ["products.filter(product=>product.category==='bundle-deals')","promotionSection.hidden=!promotionProducts.length","p.category!=='bundle-deals'","catalogRoots().flatMap","#homeDigitalProductsGrid"])assert.ok(catalog.includes(token),token);
assert.ok(api.includes('p.category'));assert.match(roadmap,/Home Bundle Deals Catalog/);console.log('v0.14.210 home bundle-deals catalog frontend/backend events: PASS');
