# PATCH v0.14.126 — Vision 5 Navigation and Wording Cleanup

วันที่: 2026-08-11

## ผลลัพธ์

- ใช้คำเดียวทั้งระบบว่า `ตะกร้าคอร์ส` และ `ตะกร้าคอร์สของฉัน` โดยนำคำว่า `ร่างตะกร้า` และคำกำกวมออกจากหน้าผู้ใช้
- ปุ่มบนการ์ดตะกร้าเหลือคำสั่งที่สั้นและตรงงาน: `จัดการบทเรียนและ EP`, `แก้ไขตะกร้า`, `ส่งตรวจ`
- ปรับข้อความตอบกลับ API ให้ตรงกับหน้าจอ โดยไม่เปลี่ยนสถานะภายในหรือกติกาหัก 1 เครดิต
- อัปเดต cache identity ของหน้า Vision 5 เป็น `014126` เพื่อให้ Header และปุ่มกลางชุดล่าสุดถูกโหลดจริง
- เพิ่ม regression test ป้องกันคำเก่าและ asset key เก่ากลับมา

## ขอบเขตที่ไม่เปลี่ยน

- ไม่เปลี่ยน API contract, ฐานข้อมูล, เครดิต, การตรวจโดย Boss หรือกฎล็อกหลังมียอดขาย
- ไม่เปิดเมนูงานรองที่ถูกซ่อนไว้ก่อนหน้า
