# VisionD v0.14.114 / V Easy v1.0.13 — Activation Simple Request

Activation now sends JSON as `text/plain;charset=UTF-8`, a CORS-safelisted request that does not require an OPTIONS preflight. The server continues parsing the JSON body normally. Retry fields and structured errors from v0.14.113 remain intact.

Deploy Web before installing the APK.

EVENT CASE: ยังไม่เสร็จ — ต้องยืนยัน activation จริงบน Redmi
