# PATCH v0.14.315 — Paper Doll Unlimited Source

## แก้อะไร

- ยกเลิกเพดาน 200 MB ของ PNG, PDF และ ZIP ต้นทางในเครื่องมือสร้างตะกร้าตุ๊กตากระดาษ
- แสดงข้อความชัดเจนว่าไฟล์ต้นทางไม่ถูกจำกัดขนาด
- คงเพดานไฟล์ PDF ผลลัพธ์ต่อตะกร้าไว้ต่ำกว่า API และแนะนำให้เพิ่มจำนวนตะกร้าเมื่อผลลัพธ์ใหญ่เกิน

## ขอบเขตที่ไม่เปลี่ยน

- การแบ่งหน้าไม่ซ้ำและไม่ตกหล่น
- สร้างเป็นร่าง หมวด `paper-doll` และใช้ Slug `paper-doll-NNN`
- ไม่ Push และไม่ Deploy

## ย้อนกลับ

`git revert COMMIT_ID`
