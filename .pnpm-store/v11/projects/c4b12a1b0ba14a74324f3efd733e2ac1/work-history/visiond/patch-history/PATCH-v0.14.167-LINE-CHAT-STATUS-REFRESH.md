# Patch v0.14.167 — LINE Chat Status Refresh

Bot readiness now returns explicit running and per-channel readiness state. V Easy v1.0.21 normalizes persisted `status=connected` rows, refreshes after app resume/focus, and offers a manual retry without asking for LINE credentials again.
