# PATCH v0.14.153 — Overlay-only Contract

- แพตนี้และแพตต่อไปติดตั้งด้วยการแตก ZIP แล้ววางทับอย่างเดียว
- ไม่มี PowerShell, CMD, คำสั่งลบ, การย้ายโฟลเดอร์ หรือการเปลี่ยน Cloudflare
- `visiond-mvp` เป็น Production tree ถาวรและได้รับงาน 149–153 โดยตรง
- ไฟล์เก่าที่การวางทับไม่ลบจะถูกปล่อยไว้ หากไม่ถูก Runtime เรียกใช้
