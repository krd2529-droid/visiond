const memberDate=value=>new Date(String(value).replace(' ','T')+'Z').toLocaleDateString('th-TH',{day:'numeric',month:'long',year:'numeric'});
const memberEsc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
async function loadMemberRights(){const box=document.querySelector('#dashboardMemberships');if(!box)return;const response=await fetch('/api/member/plans',{cache:'no-store'}),data=await response.json().catch(()=>({}));if(!response.ok||!data.memberships?.length){box.innerHTML='<div class="dashboard-card"><b>ยังไม่มีสิทธิ์ Member รายหมวด</b><p>เลือกแพ็กเกจรายเดือนหรือรายปีเพื่อดาวน์โหลดสินค้าทั้งหมวด</p><a class="primary-button" href="/member">ดูแพ็กเกจ Member</a></div>';return}box.innerHTML=`<div class="dashboard-card membership-dashboard-card"><h2>Member ที่กำลังใช้งาน</h2>${data.memberships.map(item=>`<a href="/digital-products?category=${encodeURIComponent(item.category_slug)}"><b>${memberEsc(item.category_name||item.category_slug)}</b><span>ใช้ได้ถึง ${memberDate(item.expires_at)} · เปิดดูสินค้าทั้งหมวด →</span></a>`).join('')}<a class="secondary-button" href="/member">ต่ออายุหรือดูแพ็กเกจอื่น</a></div>`}
loadMemberRights();
