# Patch v0.14.323 — แก้ผลตรวจทั้งระบบก่อนเริ่ม Feature Map

## ขอบเขต

- เปลี่ยน Regression runner ให้ตรวจสัญญาฟังก์ชันกลางและเทสต์ของเวอร์ชันปัจจุบันจาก `VERSION.txt`
- เปลี่ยนตัวตรวจประวัติเอกสารให้ยืนยันเอกสารและ ledger ปัจจุบัน โดยไม่บังคับลบเอกสารเก่าที่ยังเก็บไว้
- แก้ `START-HERE.md` ทั้งสองตำแหน่งให้ชี้เวอร์ชันและ Patch ledger ปัจจุบัน
- เติม cache key 6 จุดใน `ads-center`, `elon-page-admin` และ `my-programs`
- เปลี่ยนจุดที่กลืนข้อผิดพลาด 3 จุดให้มีรหัส Log โดยคง fallback และพฤติกรรมธุรกิจเดิม

## สิ่งที่ห้ามกระทบ

- ไม่เปลี่ยนกฎราคา สิทธิ์ การชำระเงิน การอนุมัติ หรือสถานะสินค้า
- ไม่ Push และไม่ Deploy
- ยังไม่เริ่มสร้าง Feature Map

## Rollback

- Commit ก่อนหน้า: `24b3c37`
