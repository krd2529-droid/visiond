# v0.14.109 — VBot Key Storefront

Event Case เพิ่มท้ายรอบ: ในตะกร้า ลูกค้าเปลี่ยนอายุคีย์ 30 วัน / 1 ปี / ตลอดอายุและเห็นราคาที่เปลี่ยนตามตัวเลือกได้ โดยคีย์ทดสอบยังอยู่เฉพาะหลังบ้านและไม่ออกหน้าร้าน

ตามคำสั่ง Boss ให้ลบข้อมูลที่เจนเล่น migration `0031_remove_test_credit_order.sql` ลบเฉพาะออเดอร์ `VD-CREDIT-1786366955307-50BE` และคีย์ V Easy ที่ลงท้าย `F7HJ`, `UE65`, `IF6X` พร้อมข้อมูลลูกที่เกี่ยวข้อง ไม่ใช้เงื่อนไขกว้างกับข้อมูลลูกค้าจริง

เมนูหลักใช้ชื่อ `VBot` และเปิด `bots.html` เป็นหน้ารวมกับรายละเอียดแอปในระดับเดียว รายการที่เข้าตะกร้าคือคีย์ 30 วัน 1 ปี หรือตลอดชีพ

แพ็กเกจจะแสดงและขายได้เฉพาะรหัสลูกค้า `monthly`, `yearly`, `lifetime` เมื่อ `offer_price` เป็นจำนวนสตางค์มากกว่า 0, plan active และ product ที่ผูกอยู่ published เท่านั้น ค่า `NULL`, 0, ติดลบ, draft รวมถึง plan ทดสอบ/trial/admin/unknown จะไม่ออก public API และสั่งซื้อไม่ได้

แต่ละตะกร้าคีย์ใช้ product จริง `category=vbot-key` และ `product_kind=vision7-key` เพื่อใช้ order/payment/fulfillment เดิม เซิร์ฟเวอร์ตรวจราคา plan ซ้ำก่อนสร้างออเดอร์และไม่ให้โปรโมชันลดราคาคีย์ การอนุมัติชำระเงินยังออกคีย์แบบ idempotent ผ่าน Vision 7 เดิม

คำสั่งล่าสุดยกเลิกแนวทางดาวน์โหลดตัวติดตั้งฟรีจาก v0.14.108: ลูกค้าต้องซื้อและได้รับอนุมัติก่อน ระบบจึงปลดทั้งคีย์และสิทธิ์ดาวน์โหลด ตัวดาวน์โหลดตรวจบัญชีเจ้าของและ license ที่ active/trial และยังไม่หมดอายุทุกครั้ง

กฎ 1 คีย์ = 1 ร้านยังจำกัดเฉพาะ V Easy; VBot แพลตฟอร์มอื่นไม่รับกฎนี้
