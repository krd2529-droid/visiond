# PATCH v0.14.373 — Meta Pixel Conversion Tracking Feature Map

- ลงทะเบียน `META-PIXEL-001` ครบ loader, PageView, commerce funnel events และ paid Purchase dedupe
- เพิ่ม runtime identifier บน script ที่ทำงานอยู่แล้ว โดยไม่เปลี่ยน Pixel ID, payload, trigger, external loader หรือ UI

`EVENT CASE: ยังไม่เสร็จ — ต้องแพตต่อ`

งานถัดไป: Coverage Audit หา runtime system ที่ยังไม่มีรหัสจากโค้ดจริง
