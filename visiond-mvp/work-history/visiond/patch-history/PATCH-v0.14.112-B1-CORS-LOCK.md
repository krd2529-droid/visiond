# VisionD v0.14.112 / V Easy v1.0.11 — Production CORS Lock

This hotfix replaces the prior static-only CORS assertion with a compiled Cloudflare Pages routing test. It adds real mobile health and scoped mobile event endpoints, completes approved WebView CORS, removes the APK dependency on the rejected custom header, and aligns bot/product contracts.

Deploy the Web ZIP before installing or testing the APK. Boss receives only two files; mobile Source stays private and versioned inside the team.

EVENT CASE: ยังไม่เสร็จ — ต้องแพตต่อจน Boss ยืนยัน ONLINE และ activation บน Redmi จริง
