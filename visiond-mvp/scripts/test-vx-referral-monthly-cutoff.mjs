import assert from'node:assert/strict';import fs from'node:fs';
const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),'utf8'),api=read('functions/api/admin/vx-referrals.js'),admin=read('public/vx-affiliate-admin.js'),page=read('public/vx-affiliate-admin.html'),referrals=read('functions/_vx_referrals.js');
const cutoff=/date\(o\.updated_at,'\+7 hours'\)<date\('now','\+7 hours','start of month'\)/g;assert.ok((api.match(cutoff)||[]).length>=3);assert.match(api,/paid_before_bangkok_month_start/);assert.match(api,/clear_day:1/);assert.match(referrals,/MAX\(0,\?\)\*c\.rate_bps\/10000/);assert.match(admin,/จ่ายทุกวันที่ 1/);assert.match(admin,/ยอดตั้งแต่วันที่ 1 เข้ารอบถัดไป/);assert.match(admin,/item\.base_amount/);assert.match(page,/ยอดชำระเต็มก่อนเข้าวันที่ 1/);assert.match(page,/vx-affiliate-admin\.js\?v=2/);
console.log('VX referral monthly cutoff: PASS');
