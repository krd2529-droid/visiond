import assert from'node:assert/strict';import fs from'node:fs';
const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js'),html=read('public/tiktok-analyzer.html'),css=read('public/tiktok-analyzer.css');assert.match(client,/renderPendingShopDashboard/);assert.match(client,/รอข้อมูลหลังอนุญาต API/);assert.match(client,/renderLiveShopDashboard/);assert.match(client,/colspan="7"/);assert.match(html,/ดูโครงฟีเจอร์/);assert.match(css,/pending-chart/);assert.equal(read('VERSION.txt').trim(),'v0.14.495');console.log('TikTok API-pending review layout regression: PASS');
