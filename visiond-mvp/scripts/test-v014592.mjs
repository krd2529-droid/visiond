import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),ui=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css'),featureMap=read('FEATURE-MAP.md');assert.equal(read('VERSION.txt').trim(),'v0.14.592');assert.match(html,/tiktok-analyzer\.css\?v=014592/);assert.match(html,/tiktok-analyzer\.js\?v=014592/);assert.match(html,/v0\.14\.592/);for(const token of['shopProductSummaryTable(products,orders)','<th>ลำดับ</th>','<th>สินค้า</th>','<th>รหัสสินค้า</th>','<th>ยอดขาย</th>','<th>ค่าคอม</th>','shopProductSummaryTable(products,orders)'])assert.ok(ui.includes(token),token);assert.ok(!ui.includes("products.slice(0,20).map(x=>shopProductLabel(x,orders)).join('<br>')"),'old 20-row plaintext summary must be removed');for(const token of['.shop-product-table-wrap{max-height:430px','overflow:auto','.shop-product-table th{position:sticky','tbody tr:nth-child(even)'])assert.ok(css.includes(token),token);assert.match(featureMap,/แสดงสินค้าที่ซิงก์ทั้งหมดเป็นตารางหัวคอลัมน์แบบเลื่อนได้/);console.log('v0.14.592 TikTok Shop product summary table checks passed');
