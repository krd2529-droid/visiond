import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),ui=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');for(const token of['<th>ลำดับ</th>','<th>รหัสสินค้า</th>'])assert.ok(ui.includes(token),token);assert.ok(!ui.includes("products.slice(0,20).map(x=>shopProductLabel(x,orders)).join('<br>')"),'old 20-row plaintext summary must be removed');for(const token of['.shop-product-table-wrap{max-height:430px','overflow:auto','.shop-product-table th{position:sticky','tbody tr:nth-child(even)'])assert.ok(css.includes(token),token);console.log('v0.14.592 TikTok Shop product summary table checks passed');
