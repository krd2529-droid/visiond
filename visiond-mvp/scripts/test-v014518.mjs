import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(provider,/daily_product_list \(array of \{rank,product,product_type,ranking_group,ranking_score,ranking_reason\}\)/);for(const value of['best_seller=ขายดี','core=สินค้าหลัก','secondary=สินค้ารอง','test=สินค้าเทส','remove=สินค้าที่ควรเอาออก'])assert.match(provider,new RegExp(value));assert.match(provider,/ให้กลุ่ม remove อยู่ท้ายลิสต์/);assert.match(html,/แบ่งเป็น 5 กลุ่ม: ขายดี · สินค้าหลัก · สินค้ารอง · สินค้าเทส · สินค้าที่ควรเอาออก/);assert.match(client,/rankingGroups=\{best_seller:'ขายดี',core:'สินค้าหลัก',secondary:'สินค้ารอง',test:'สินค้าเทส',remove:'ควรเอาออก'\}/);assert.match(client,/ranking_group/);assert.match(client,/groupCounts/);assert.match(client,/\['A','B','C','D','E','F'\]/);assert.match(css,/\.ranking-group-tag/);assert.match(css,/\.product-prep-item\.group-remove/);assert.equal(read('VERSION.txt').trim(),'v0.14.518');console.log('TikTok five ranking groups regression: PASS');
