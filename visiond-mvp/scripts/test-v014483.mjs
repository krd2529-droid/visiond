import fs from'node:fs';import assert from'node:assert/strict';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),api=read('functions/api/admin/tiktok-analyzer/index.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js');assert.match(html,/name="lookback_days"/);for(const day of[7,14,30,60,90])assert.match(html,new RegExp(`value="${day}"`));assert.match(provider,/median ของคลิป/);assert.match(provider,/traffic_status=good/);assert.match(provider,/no_traffic/);assert.match(provider,/คลิปใหม่กว่า 72 ชั่วโมง/);assert.match(provider,/clip_performance/);assert.match(api,/lookbackDays/);assert.match(client,/trafficLabels/);assert.match(client,/clipPerformance/);assert.match(read('VERSION.txt').trim(),/^v0\.14\.\d+$/);console.log('v0.14.483 historical clip traffic analysis: PASS');
