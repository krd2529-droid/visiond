import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),schema=read('functions/_tiktok_analyzer.js'),api=read('functions/api/admin/tiktok-analyzer/index.js'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(schema,/archived_at TEXT/);assert.match(api,/action==='delete_channel'/);assert.match(api,/created_by=\?/);assert.match(api,/UPDATE tiktok_channels SET archived_at=CURRENT_TIMESTAMP/);assert.doesNotMatch(api,/DELETE FROM tiktok_channels/);assert.match(api,/WHERE c\.archived_at IS NULL/);assert.match(client,/class="delete-channel"/);assert.match(client,/กู้คืนได้/);assert.match(css,/\.delete-channel/);assert.equal(read('VERSION.txt').trim(),'v0.14.509');console.log('TikTok recoverable saved-channel removal regression: PASS');
