import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const[v,i,a,api,media]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('functions/api/admin/v12-broadcast.js'),read('functions/api/media/v12-connect/broadcast/[file].js')]);assert.equal(v.trim(),'v0.14.204');assert.match(i,/WEB v0\.14\.204/);assert.match(a,/ADMIN v0\.14\.204/);for(const x of['facebookAttachment','message_attachments','attachment_id','external_account_id'])assert.ok(api.includes(x),x);assert.match(media,/172800000/);console.log('v0.14.204 broadcast image delivery: PASS');
