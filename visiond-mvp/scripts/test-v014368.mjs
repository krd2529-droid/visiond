import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');assert.equal(r('VERSION.txt').trim(),'v0.14.368');assert.match(r('public/index.html'),/WEB v0\.14\.368/);assert.match(r('public/admin.html'),/ADMIN v0\.14\.368/);const html=r('public/course-center.html'),ui=r('public/course-center.js'),api=r('functions/api/course-seller/sales.js'),m=r('FEATURE-MAP.md');assert.match(html,/class="seller-card course-sales-dashboard" data-feature="COURSE-SALES-001"/);for(const x of['COURSE-SALES-001','paid','สูงสุด 60 วัน','CAST(total / 2 AS INTEGER)','clear_day=1','สูงสุด 500'])assert.ok(m.includes(x),x);assert.match(ui,/\/api\/course-seller\/sales\?\$\{params\}/);assert.match(ui,/ไม่หักค่า API/);assert.match(api,/requireUser\(ctx\)/);assert.match(api,/o\.course_owner_user_id=\?/);assert.match(api,/o\.status='paid'/);assert.match(api,/date\(o\.updated_at,'\+7 hours'\)/);assert.match(api,/if \(days > 60\)/);assert.match(api,/const partnerTeacherSql = "CAST\(o\.total \/ 2 AS INTEGER\)"/);assert.match(api,/clear_day: 1/);assert.match(api,/LIMIT 500/);assert.match(api,/"cache-control": "no-store"/);console.log('PASS v0.14.368 Course Seller Sales Reporting');
