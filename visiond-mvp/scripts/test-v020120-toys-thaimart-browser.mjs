import assert from'node:assert/strict';
import fs from'node:fs';
import http from'node:http';
import path from'node:path';
import{createRequire}from'node:module';
import{fileURLToPath}from'node:url';
const require=createRequire(import.meta.url);let chromium;for(const candidate of[process.env.PLAYWRIGHT_PACKAGE,'C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright','playwright'].filter(Boolean)){try{({chromium}=require(candidate));break}catch{}}if(!chromium)throw new Error('Playwright Chromium is required');
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),publicRoot=path.join(root,'public'),searchCalls=new Map(),writes=[];
const item={id:7,meta_id:'THAI-007',slug:'thai-007',title:'โมเดล <img id="hostile"> Thai Mart 🚀',description:'รายละเอียด\nบรรทัดสอง',availability:'in stock',condition:'used',price:1850,cost:777.77,quantity:0,brand:'VisionD',product_line:'Transformers',series:'Studio',status:'published',gtin:'8851234567890',shopee_weight_kg:.5,shopee_package_width_cm:12,shopee_package_length_cm:20,shopee_package_height_cm:8,visiond_cover_index:1,visiond_image_urls:['/fixture/cover.svg'],image_1_url:'/fixture/cover.svg',image_2_url:'/fixture/meta.svg',product_tags:'[]'};
const handoff={schema:'visiond.thaimart-product-handoff',version:1,platform:'thaimart',product:{visiond_product_id:7,category_path:['งานอดิเรกและของสะสม','โมเดลและของสะสม','โมเดล / บอร์ดเกม / การ์ดเกม'],images:[{url:'https://visiondonline.com/api/toys-center/gallery/7/1?v=2',cover:true}],name:item.title,description:item.description,sku:item.meta_id,sales:{currency:'THB',price_thb:1850,stock:0},shipping:{weight_g:500,dimensions_cm:{width_cm:12,length_cm:20,height_cm:8}}},validation:{complete:true,missing:[]}};
const readBody=request=>new Promise((resolve,reject)=>{const chunks=[];request.on('data',chunk=>chunks.push(chunk));request.on('end',()=>resolve(Buffer.concat(chunks)));request.on('error',reject)}),send=(response,data,status=200)=>{response.statusCode=status;response.setHeader('content-type','application/json; charset=utf-8');response.end(JSON.stringify(data))};
const server=http.createServer(async(request,response)=>{const url=new URL(request.url,'http://127.0.0.1');if(url.pathname==='/api/admin/toys-center'&&request.method==='GET')return send(response,{settings:{storefront_mode:'public'},items:[item],pagination:{page:1,limit:25,total:1},permissions:{can_manage_orders:false}});if(url.pathname==='/api/admin/toys-center/7/images')return send(response,{cover_position:1,items:[0,1].map(position=>({position,url:`/fixture/vision-${position}.svg`}))});if(url.pathname==='/api/admin/toys-center/thaimart-products'){const query=url.searchParams.get('q')||'';searchCalls.set(query,(searchCalls.get(query)||0)+1);if(query==='alpha')await new Promise(resolve=>setTimeout(resolve,900));if(query==='beta')await new Promise(resolve=>setTimeout(resolve,100));const rows=query==='empty'?[]:[{id:7,title:`${query} ${item.title}`,meta_id:item.meta_id,status:'published',updated_at:'2026-09-16 00:00:00'}];return send(response,{items:rows,pagination:{limit:24,has_more:false,next_cursor:null}})}if(url.pathname==='/api/admin/toys-center/7/thaimart-export')return send(response,handoff);if(url.pathname==='/api/admin/toys-center/7'&&request.method==='PUT'){writes.push(await readBody(request));return send(response,{ok:true,id:7,message:'แก้ไขสินค้า Toys Center สำเร็จ'})}if(url.pathname.startsWith('/fixture/')){response.setHeader('content-type','image/svg+xml');return response.end('<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100"><rect width="100" height="100" fill="#0abab5"/></svg>')}const relative=url.pathname==='/'?'/toys-center-admin.html':url.pathname,local=path.resolve(publicRoot,`.${relative}`);if(!local.startsWith(publicRoot)||!fs.existsSync(local)){response.statusCode=404;return response.end('not found')}const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml'};response.setHeader('content-type',types[path.extname(local)]||'application/octet-stream');response.end(fs.readFileSync(local))});
await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));const base=`http://127.0.0.1:${server.address().port}`,browser=await chromium.launch({headless:true,channel:process.env.PLAYWRIGHT_CHANNEL||'chrome'});
try{
  const page=await browser.newPage({viewport:{width:1280,height:1000}});await page.goto(`${base}/toys-center-admin.html`);await page.waitForSelector('.product-row');assert.equal(await page.locator('#thaiMartWorkspace').count(),1);assert.match(await page.locator('#thaiMartWorkspace').textContent(),/โมเดล \/ บอร์ดเกม \/ การ์ดเกม/);assert.match(await page.locator('.shopee-shipping').textContent(),/Thai Mart/);
  await page.locator('#thaiMartSearchInput').fill('alpha');await page.waitForFunction(()=>document.querySelector('#thaiMartSearchStatus').textContent.includes('กำลังค้นหา'));await page.locator('#thaiMartSearchInput').fill('beta');await page.waitForTimeout(340);await page.locator('#thaiMartSearchInput').fill('alpha');await page.waitForFunction(()=>document.querySelector('.thaimart-result strong')?.textContent.startsWith('alpha'),null,{timeout:3000});assert.equal(searchCalls.get('alpha'),1,'identical Thai Mart A request joins inflight');assert.equal(searchCalls.get('beta'),1);assert.match(await page.locator('.thaimart-result strong').textContent(),/^alpha/,'Thai Mart A→B→A publishes latest intent');
  await page.locator('.thaimart-result').click();await page.waitForFunction(()=>document.querySelector('#thaiMartJsonText').textContent.includes('visiond.thaimart-product-handoff'));assert.equal(await page.locator('#thaiMartPreview img').count(),0,'preview renders hostile title as text only');assert.match(await page.locator('#thaiMartValidationStatus').textContent(),/ข้อมูลครบ/);const preview=JSON.parse(await page.locator('#thaiMartJsonText').textContent());assert.deepEqual(preview,handoff);assert.equal(preview.product.sales.stock,0);
  const downloadPromise=page.waitForEvent('download');await page.click('#downloadThaiMartJson');const download=await downloadPromise;assert.equal(download.suggestedFilename(),'visiond-thaimart-v1-7.json');assert.deepEqual(JSON.parse(fs.readFileSync(await download.path(),'utf8')),handoff);await page.click('#clearThaiMartProduct');assert.equal(await page.locator('#thaiMartPreview').isHidden(),true);
  await page.locator('#thaiMartSearchInput').fill('alpha');await page.waitForFunction(()=>document.querySelector('.thaimart-result'));await page.locator('.thaimart-result').click();await page.waitForFunction(()=>document.querySelector('#thaiMartJsonText').textContent.includes('thaimart'));await page.getByRole('button',{name:'แก้ไข',exact:true}).click();await page.waitForFunction(()=>document.querySelectorAll('#preview1 .visiond-cover-choice').length===2);const before=searchCalls.get('alpha');const refreshed=page.waitForResponse(response=>response.url().includes('/api/admin/toys-center/thaimart-products?')&&new URL(response.url()).searchParams.get('q')==='alpha');await page.getByRole('button',{name:'บันทึกการแก้ไข'}).click();await refreshed;assert.equal(writes.length,1);assert.ok(searchCalls.get('alpha')>before,'product mutation invalidates active Thai Mart namespace');assert.equal(await page.locator('#thaiMartPreview').isHidden(),true,'selected changed product preview is cleared');
  await page.locator('#thaiMartSearchInput').fill('empty');await page.waitForFunction(()=>document.querySelector('#thaiMartSearchStatus').textContent.includes('ไม่พบ'));assert.equal(await page.locator('.thaimart-result').count(),0);
  const mobile=await browser.newPage({viewport:{width:390,height:844}});await mobile.goto(`${base}/toys-center-admin.html`);await mobile.waitForSelector('#thaiMartWorkspace');assert.equal(await mobile.evaluate(()=>document.documentElement.scrollWidth),390);assert.equal(await mobile.locator('#thaiMartWorkspace .shopee-page-map article').count(),2);
  console.log('PASS v0.20.120 installed Chrome Thai Mart debounce/dedupe/stale guard, safe preview/download, targeted invalidation and 390px layout');
}finally{await browser.close();await new Promise(resolve=>server.close(resolve))}
