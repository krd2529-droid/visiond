import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(html,/>ค่าคอมทุกช่อง</);assert.match(html,/>วิเคราะห์ช่อง</);assert.match(html,/id="channelShopAnalysis"/);assert.match(html,/ค่าคอมมิชชันรวมและแยกทุกช่อง/);assert.match(client,/ผลวิเคราะห์ช่อง/);assert.match(css,/output-overview #channelShopAnalysis/);assert.match(css,/output-channel #channelShopAnalysis/);assert.equal(read('VERSION.txt').trim(),'v0.14.505');console.log('TikTok commission portfolio and channel analysis split regression: PASS');
