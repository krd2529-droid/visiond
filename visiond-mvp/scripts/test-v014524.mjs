import assert from 'node:assert/strict';import fs from 'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),analyzer=read('functions/api/admin/tiktok-analyzer/index.js'),connections=read('functions/api/admin/tiktok-connections/index.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');for(const text of ['A=ขายดีโดยมียอดขายดี วิวดี และทราฟฟิกดี','B=สินค้ารองที่วิวดีและยังขายได้บ้าง','C=สินค้าใหม่หรือสินค้าเทสที่ต้องเฝ้าสังเกต','D=สินค้ากระแส สินค้าตามฤดูกาล หรือสินค้าที่ใช้จัดโปรโมชั่น','F=สินค้าที่วิวไม่ดีและขายไม่ได้ควรคัดออก'])assert.ok(provider.includes(text));assert.match(provider,/product_type='F' WHERE product_type='E'/);assert.match(provider,/product_grade='F' WHERE product_grade='E'/);assert.match(analyzer,/\^\[A-DF\]\$/);assert.match(analyzer,/instr\('ABCDF',product_type\)/);assert.match(analyzer,/score<40\?'F'/);assert.match(connections,/shop_remove_f/);assert.match(connections,/product_grade='F'/);assert.doesNotMatch(connections,/shop_remove_e/);assert.match(client,/const typeLabels=\{A:'ขายดี · วิวและทราฟฟิกดี'.+F:'วิวไม่ดี · ขายไม่ได้ · คัดออก'\}/);assert.match(client,/'ABCDF'/);assert.match(client,/remove-f-check/);assert.doesNotMatch(client,/remove-e-check|removeShowcaseE/);assert.match(html,/A–D และ F/);assert.match(html,/id="removeShowcaseF"/);assert.doesNotMatch(html,/A–E|removeShowcaseE/);assert.match(css,/\.grade-F/);assert.equal(read('VERSION.txt').trim(),'v0.14.524');console.log('TikTok A B C D F grading regression: PASS');
