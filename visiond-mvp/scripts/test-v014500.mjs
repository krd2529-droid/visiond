import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(client,/AUTOMATIC · TIKTOK SHOP API/);assert.match(client,/MANUAL ANALYSIS · ภาพและข้อมูลที่กรอกเอง/);assert.match(client,/ไม่ใช่ยอดจาก Showcase อัตโนมัติ/);assert.match(client,/เกรดสินค้า A–F จากภาพและข้อมูลที่กรอก/);assert.match(css,/manual-source-note/);assert.equal(read('VERSION.txt').trim(),'v0.14.500');console.log('TikTok manual-vs-API source labels regression: PASS');
