import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js');assert.match(client,/setWorkspaceView\('input'\)/);assert.doesNotMatch(client,/setWorkspaceView\(reviewDemo\?'output':'input'\)/);assert.equal(read('VERSION.txt').trim(),'v0.14.508');console.log('TikTok demo-live initial view parity regression: PASS');
