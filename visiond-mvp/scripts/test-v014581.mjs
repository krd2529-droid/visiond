import fs from'node:fs';import assert from'node:assert/strict';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/toys-center-admin.html');assert.equal(read('VERSION.txt').trim(),'v0.14.581');assert.match(read('public/index.html'),/WEB v0\.14\.581/);assert.match(read('public/admin.html'),/ADMIN v0\.14\.581/);for(const label of['ID / SKU (รหัสสินค้า)','Slug (ชื่อย่อในลิงก์)','Availability','Condition','Google Category (หมวดหมู่สินค้า Google)','Facebook Category (หมวดหมู่สินค้า Facebook)','GTIN (เลขบาร์โค้ดสากล)','Item Group ID (รหัสกลุ่มสินค้ารุ่นเดียวกัน)','Gender (เพศ)','Color (สี)','Size (ขนาด)','Age Group (ช่วงอายุ)','Material (วัสดุ)','Pattern (ลวดลาย)','Product Tags (แท็กสินค้า'])assert.ok(html.includes(label),label);assert.match(html,/value="in stock">มีสินค้า/);assert.match(html,/value="used">มือสอง/);assert.match(html,/v0\.14\.581/);console.log('v0.14.581 bilingual Toys Center labels checks passed');
