import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js');assert.match(client,/const demoChannels=Array\.from/);assert.match(client,/function selectDemoChannel/);assert.match(client,/data-demo-id/);assert.doesNotMatch(client,/demo-channel.*disabled/);assert.match(client,/if\(state\.selected&&!reviewDemo\)/);assert.equal(read('VERSION.txt').trim(),'v0.14.507');console.log('TikTok demo-real channel selection parity regression: PASS');
