import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const[v,i,a,ui,api,media,migration,roadmap]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('public/v12-connect.js'),read('functions/api/admin/v12-connect.js'),read('functions/api/media/v12-connect/outbound/[[path]].js'),read('migrations/0045_v12_connect_performance_media.sql'),read('VISIOND-ROADMAP.md')]);assert.equal(v.trim(),'v0.14.200');assert.match(i,/WEB v0\.14\.200/);assert.match(a,/ADMIN v0\.14\.200/);assert.match(ui,/setInterval[\s\S]*4000/);assert.doesNotMatch(api,/ensureDatabase|ensureVEasyRuntimeSchema|ensureV12Schema/);assert.match(media,/v12-connect\\\/outbound/);assert.match(migration,/veasy_conversation_controls/);assert.match(roadmap,/v0\.14\.200[\s\S]*Faster Inbox/);console.log('v0.14.200 V12 performance and outbound images: PASS');
