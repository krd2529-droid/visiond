import fs from 'node:fs';import assert from 'node:assert/strict';const read=file=>fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8'),html=read('public/admin-courses.html'),ui=read('public/admin-courses.js'),api=read('functions/api/admin/courses/[id]/lessons.js');
assert.equal(read('VERSION.txt').trim(),'v0.14.438');assert.match(read('public/index.html'),/WEB v0\.14\.438/);assert.match(read('public/admin.html'),/ADMIN v0\.14\.438/);assert.match(html,/admin-courses\.js\?v=014438/);assert.doesNotMatch(html,/name="title" required[^>]*placeholder="เว้นว่างได้/);assert.match(html,/placeholder="เว้นว่างได้ ระบบจะตั้งชื่อ EP ตามลำดับ"/);assert.match(ui,/createDraft\(\{allowPartial:true\}\)/);assert.match(ui,/fallbackTitle=`EP \$\{/);assert.match(ui,/form\.set\('draft_only','1'\)/);assert.match(api,/draftOnly/);console.log('PASS v0.14.438 add EP without required-field lock');
