import fs from 'node:fs';import assert from 'node:assert/strict';const read=file=>fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8'),html=read('public/admin-courses.html'),ui=read('public/admin-courses.js');
assert.equal(read('VERSION.txt').trim(),'v0.14.435');assert.match(read('public/index.html'),/WEB v0\.14\.435/);assert.match(read('public/admin.html'),/ADMIN v0\.14\.435/);assert.match(html,/admin-courses\.js\?v=014435/);assert.equal((html.match(/name="payment_account" type="radio"/g)||[]).length,2);assert.equal((html.match(/class="course-payment-choice/g)||[]).length,1);assert.doesNotMatch(html,/id="coursePaymentForm"/);assert.doesNotMatch(ui,/coursePaymentForm/);assert.match(ui,/courseForm\.elements\.payment_account\.value/);assert.match(ui,/\/payment`/);assert.match(ui,/บันทึกร่างข้อมูลคอร์ส EP และบัญชีรับเงินล่าสุดแล้ว/);console.log('PASS v0.14.435 single company course payment selector');
