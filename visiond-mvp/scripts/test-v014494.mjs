import assert from'node:assert/strict';import fs from'node:fs';
const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css'),notes=read('PATCH-v0.14.494-tiktok-review-demo-ui.md');
assert.match(html,/review_demo=1/);assert.match(html,/ข้อมูลตัวอย่างสำหรับการตรวจสอบ/);assert.match(html,/v0\.14\.494/);assert.match(client,/reviewDemo/);assert.match(client,/demoShopData/);assert.match(client,/review_demo:true/);assert.match(client,/demo\?'disabled'/);assert.match(client,/ค่าคอมมิชชันรายวัน/);assert.match(client,/เปรียบเทียบแต่ละช่อง/);assert.match(css,/commission-kpis/);assert.match(css,/showcase-table/);assert.match(css,/grade-badge/);assert.match(notes,/never claims that TikTok API authorization succeeded/);assert.equal(read('VERSION.txt').trim(),'v0.14.494');console.log('TikTok reviewer demo UI regression: PASS');
