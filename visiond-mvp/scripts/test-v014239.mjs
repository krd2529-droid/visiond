import assert from'node:assert/strict';import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),v=r('VERSION.txt'),p=r('public/v12-connect.html'),u=r('public/v12-connect.js');assert.equal(v.trim(),'v0.14.239');assert.match(p,/v0\.14\.239/);for(const x of["search=byId('v12Search')","search.addEventListener('input',renderList)",'document.activeElement===search','30000'])assert.ok(u.includes(x),x);assert.doesNotMatch(u,/v12Search\.oninput/);console.log('v0.14.239 stable V12 search and refresh: PASS');
