import fs from 'node:fs';import assert from 'node:assert/strict';const read=file=>fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8'),html=read('public/admin-courses.html'),css=read('public/course-seller.css');
assert.equal(read('VERSION.txt').trim(),'v0.14.437');assert.match(read('public/index.html'),/WEB v0\.14\.437/);assert.match(read('public/admin.html'),/ADMIN v0\.14\.437/);assert.match(html,/admin-courses\.js\?v=014437/);const actions=html.indexOf('class="company-course-final-actions"'),save=html.indexOf('id="saveCourseDraft"'),publish=html.indexOf('id="publishCompanyCourse"'),lessons=html.indexOf('id="lessonList"');assert.ok(actions>lessons&&save>actions&&publish>save,'draft and publish actions must be paired after lesson list');assert.equal((html.match(/id="saveCourseDraft"/g)||[]).length,1);assert.match(css,/\.company-course-final-actions \{/);assert.match(css,/grid-template-columns: minmax\(0, 1fr\) minmax\(0, 1fr\)/);assert.match(css,/@media \(max-width: 620px\)/);console.log('PASS v0.14.437 draft and publish actions paired at course basket footer');
