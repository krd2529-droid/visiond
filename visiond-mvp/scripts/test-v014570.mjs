import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const root=new URL('../',import.meta.url),read=p=>readFile(new URL(p,root),'utf8');const [changes,catalog,migration,version]=await Promise.all(['functions/api/partner/v1/products/changes.js','functions/_partner_catalog.js','migrations/0072_partner_product_change_read_budget.sql','VERSION.txt'].map(read));assert.match(changes,/WITH changes AS/);assert.match(changes,/selected AS/);assert.match(changes,/product_ids AS \(SELECT DISTINCT product_id FROM selected\)/);assert.match(changes,/FROM product_ids x JOIN product_files/);assert.doesNotMatch(changes,/SELECT COUNT\(\*\) FROM product_files own WHERE own\.product_id=p\.id/);assert.match(catalog,/idx_partner_product_changes_product/);assert.match(migration,/partner_product_changes\(product_id\)/);assert.equal(version.trim(),'v0.14.570');console.log('v0.14.570 partner change-feed read budget checks passed');
