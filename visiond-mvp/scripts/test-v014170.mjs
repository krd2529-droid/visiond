import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(p,'utf8');assert.equal(r('VERSION.txt').trim(),'v0.14.170');const schema=r('functions/_veasy_shop.js'),shop=r('functions/api/vision7/shops/[shopId]/index.js'),products=r('functions/api/vision7/shops/[shopId]/products.js'),upload=r('functions/api/vision7/shops/[shopId]/products/[productId]/image.js');for(const field of ['bank_name','bank_account_name','bank_account_number','promptpay_id','cover_image_key'])assert.match(schema,new RegExp(field));for(const field of ['bankName','bankAccountName','bankAccountNumber','promptPayId'])assert.match(shop,new RegExp(field));assert.match(products,/coverImageUrl/);assert.match(upload,/5\*1024\*1024/);assert.match(upload,/ctx\.env\.FILES\.put/);console.log('PASS v0.14.170 V Easy checkout payment and product image contract');
