import fs from'node:fs';import assert from'node:assert/strict';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),api=read('functions/api/admin/tiktok-analyzer/index.js'),provider=read('functions/_tiktok_analyzer.js');assert.match(provider,/CREATE TABLE IF NOT EXISTS tiktok_channel_products/);assert.match(provider,/winner_products \(array of \{name,product_url/);assert.match(provider,/ห้ามแต่งตัวเลขหรือลิงก์/);assert.match(api,/safeUrl/);assert.match(api,/ON CONFLICT\(channel_id,name\)/);assert.match(api,/SELECT id,name,product_url,score,evidence/);assert.match(html,/ลิสต์สินค้านางฟ้าของช่องนี้/);assert.match(client,/เปิดสินค้า ↗/);assert.match(client,/ยังไม่มีลิงก์/);assert.equal(read('VERSION.txt').trim(),'v0.14.480');console.log('v0.14.480 winner product library: PASS');
