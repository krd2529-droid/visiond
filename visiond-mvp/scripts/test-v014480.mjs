import fs from'node:fs';import assert from'node:assert/strict';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),api=read('functions/api/admin/tiktok-analyzer/index.js'),provider=read('functions/_tiktok_analyzer.js');assert.match(provider,/CREATE TABLE IF NOT EXISTS tiktok_channel_products/);assert.match(provider,/winner_products \(array of \{name,product_url/);assert.match(provider,/ห้ามแต่งตัวเลขหรือลิงก์/);assert.match(api,/safeUrl/);assert.match(api,/ON CONFLICT\(channel_id,name\)/);assert.match(html,/(ลิสต์|ตาราง)สินค้าแยกแนว A–F ของช่องนี้/);assert.match(client,/เปิดสินค้า ↗/);assert.match(client,/ยังไม่มีลิงก์/);assert.match(read('VERSION.txt').trim(),/^v0\.14\.\d+$/);console.log('Winner product library regression: PASS');
