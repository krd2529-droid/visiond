import assert from 'node:assert/strict';import fs from 'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),analyzer=read('functions/api/admin/tiktok-analyzer/index.js'),connections=read('functions/api/admin/tiktok-connections/index.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(provider,/E=สินค้าที่ AI แนะนำซึ่งเป็นเพียงข้อเสนอ/);assert.doesNotMatch(provider,/product_type='F' WHERE product_type='E'|product_grade='F' WHERE product_grade='E'/);assert.match(analyzer,/\^\[A-F\]\$/);assert.match(analyzer,/instr\('ABCDEF',product_type\)/);assert.match(connections,/\^\[A-F\]\$/);assert.match(client,/E:'AI แนะนำ · เป็นเพียงข้อเสนอ'/);assert.match(client,/'ABCDEF'/);assert.match(client,/gradeCounts=\{A:0,B:0,C:0,D:0,E:0,F:0/);assert.match(html,/E สินค้าที่ AI แนะนำ เป็นเพียงข้อเสนอ/);assert.match(css,/\.grade-E\{background:#7657c8\}/);assert.match(css,/\.grade-F\{background:#e44848\}/);assert.equal(read('VERSION.txt').trim(),'v0.14.526');console.log('TikTok AI suggestion grade E regression: PASS');
