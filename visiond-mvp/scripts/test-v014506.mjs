import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js');assert.doesNotMatch(html,/id="channelOutputSelect"/);assert.doesNotMatch(client,/renderChannelOutputOptions|channelOutputSelect.*addEventListener/);assert.match(client,/Array\.from\(\{ length: 5 \}/);assert.match(client,/#channels.*addEventListener/s);console.log('TikTok left-side channel cards are the sole output picker: PASS');
