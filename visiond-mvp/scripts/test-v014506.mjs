import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js');assert.match(html,/id="channelOutputSelect"/);assert.match(html,/ช่องที่ต้องการวิเคราะห์/);assert.match(client,/renderChannelOutputOptions/);assert.match(client,/Array\.from\(\{length:5\}/);assert.match(client,/channelOutputSelect.*addEventListener/);assert.equal(read('VERSION.txt').trim(),'v0.14.506');console.log('TikTok saved-channel output picker regression: PASS');
