import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');assert.equal(r('VERSION.txt').trim(),'v0.14.342');assert.match(r('public/index.html'),/WEB v0\.14\.342/);assert.match(r('public/admin.html'),/ADMIN v0\.14\.342/);const m=r('FEATURE-MAP.md'),ui=r('public/ads-center.js'),event=r('functions/api/analytics/event.js'),ads=r('functions/api/admin/ads-center.js'),sync=r('functions/_meta_ads.js');for(const x of['ADS-ANALYTICS-001','meta_ads_insights','customer_events','CTR = clicks / impressions','data-feature="ADS-ANALYTICS-001"'])assert.ok(m.includes(x),x);assert.match(ui,/dataset\.feature='ADS-ANALYTICS-001'/);assert.match(event,/-10 seconds/);assert.match(event,/EVENTS\.has\(event\)/);assert.match(sync,/days>92/);assert.match(sync,/ON CONFLICT\(insight_date,account_id,ad_id\)/);assert.match(ads,/revenue\/spend/);assert.match(ads,/ไม่แก้ไขโฆษณาอัตโนมัติ/);console.log('PASS v0.14.342 Ads Analytics Feature Map');
