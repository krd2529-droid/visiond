import fs from 'node:fs';import assert from 'node:assert/strict';const read=file=>fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8'),html=read('public/admin-courses.html');
assert.equal(read('VERSION.txt').trim(),'v0.14.431');assert.match(read('public/index.html'),/WEB v0\.14\.431/);assert.match(read('public/admin.html'),/ADMIN v0\.14\.431/);assert.match(html,/admin-courses\.js\?v=014431/);const basketStart=html.indexOf('<section class="seller-card">'),basketEnd=html.indexOf('</section>',basketStart),lesson=html.indexOf('id="lessonManager"'),courseForm=html.indexOf('id="courseForm"'),draft=html.indexOf('id="saveCourseDraft"'),publish=html.indexOf('id="publishCompanyCourse"');assert.ok(courseForm>basketStart&&draft>courseForm&&lesson>draft&&publish>lesson&&publish<basketEnd,'EP, draft and publish must remain inside the same course basket');assert.match(html,/seller-lesson-workspace seller-lesson-embedded/);assert.equal((html.match(/id="lessonManager"/g)||[]).length,1);console.log('PASS v0.14.431 EP workspace embedded in company course basket');
