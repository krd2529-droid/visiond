import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(p,'utf8'),ai=r('functions/_veasy_line_ai.js');assert.equal(r('VERSION.txt').trim(),'v0.14.169');for(const field of ['category','short_description','full_description','specifications','warranty','shipping','price_baht','stock','has_product_image'])assert.match(ai,new RegExp(field));assert.match(ai,/Number\(value\|\|0\)\)\.toLocaleString/);assert.doesNotMatch(ai,/Number\(value\|\|0\)\)\/100/);for(const rule of ['สนใจ.+ยังไม่ยืนยันซื้อ','ห้ามขอชื่อ ที่อยู่ หรือเบอร์โทรทันที','ระบบนี้ยังไม่มีคำสั่งสร้างออเดอร์','ห้ามกล่าวว่า สั่งซื้อสำเร็จ','เชียร์ขายจากจุดเด่นจริง'])assert.match(ai,new RegExp(rule,'s'));assert.equal(ai,r('visiond-mvp/functions/_veasy_line_ai.js'));console.log('PASS v0.14.169 complete product knowledge and sales behavior contract');
