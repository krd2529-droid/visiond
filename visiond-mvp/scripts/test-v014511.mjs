import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.doesNotMatch(html,/data-list="winners"/);assert.doesNotMatch(html,/data-list="candidates"/);assert.doesNotMatch(html,/id="angelInventory"/);assert.doesNotMatch(client,/resultProductTable/);assert.doesNotMatch(client,/angelInventory|angelProducts|angelCount/);assert.doesNotMatch(css,/duplicate-product-analysis/);assert.match(html,/id="shopGradeList"/);assert.match(html,/id="removeShowcaseF"/);assert.equal(read('VERSION.txt').trim(),'v0.14.511');console.log('TikTok duplicate product analysis code removal regression: PASS');
