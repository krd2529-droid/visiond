import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),css=read('public/tiktok-analyzer.css');assert.match(css,/api-pending::after/);assert.match(css,/รอ TikTok อนุญาต API/);assert.match(css,/#b42323/);assert.equal(read('VERSION.txt').trim(),'v0.14.496');console.log('TikTok API pending red label regression: PASS');
