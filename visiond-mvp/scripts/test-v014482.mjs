import fs from'node:fs';import assert from'node:assert/strict';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),api=read('functions/api/admin/tiktok-analyzer/index.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js');assert.match(provider,/คะแนนความเหมาะสมต่ำกว่า 40\/100/);assert.match(provider,/วิวต่ำกว่าฐานปกติของช่องซ้ำหลายคลิป/);assert.match(provider,/ห้ามใช้คลิปวิวน้อยเพียงคลิปเดียว/);assert.match(api,/hasScore&&score<40\?'F'/);assert.match(client,/F:'วิวไม่ดี\/คะแนนต่ำ/);assert.match(html,/F วิวไม่ดีซ้ำหลายคลิปหรือคะแนนต่ำกว่า 40/);assert.match(read('VERSION.txt').trim(),/^v0\.14\.\d+$/);console.log('Evidence-based F products regression: PASS');
