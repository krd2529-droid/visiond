import assert from 'node:assert/strict';import fs from 'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),client=read('public/tiktok-analyzer.js'),html=read('public/tiktok-analyzer.html');for(const text of ['ตรวจสินค้าซ้ำจากทั้งชื่อและภาพสินค้า','ชื่อเหมือนกันแต่รูปสินค้า บรรจุภัณฑ์ รุ่น ขนาด หรือ SKU แตกต่างกัน ให้ถือเป็นคนละสินค้า','product_identity ต่างกัน','daily_product_list (array of {rank,product,product_identity'])assert.ok(provider.includes(text));assert.match(client,/item\?\.product_identity\|\|item\?\.product/);assert.match(html,/ชื่อและรูปสินค้าเหมือนกันจึงนับเป็นสินค้าเดียวกัน/);assert.match(html,/ชื่อเหมือนแต่รูปต่างกันให้นับแยก/);assert.equal(read('VERSION.txt').trim(),'v0.14.533');console.log('TikTok visual product identity regression: PASS');
