import assert from'node:assert/strict';import fs from'node:fs';
const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),'utf8'),ui=read('public/tiktok-analyzer.js'),card=read('public/tiktok-commission-card.js'),css=read('public/tiktok-analyzer.css');
assert.match(ui,/id="commissionShareForm"/);assert.match(ui,/name="owner"/);assert.match(ui,/name="date_from"/);assert.match(ui,/name="date_to"/);assert.match(ui,/name="caption"/);assert.match(ui,/#VX #Vtools #VisionD/);assert.match(ui,/api\('\/api\/vx\/referrals'\)/);assert.match(ui,/data-copy-vx-referral/);assert.match(ui,/data-generate-commission/);assert.match(ui,/data-social-share/);assert.match(ui,/prepareCommissionCard\(model\)/);assert.match(ui,/commissionPreparedActions/);assert.match(card,/return\{files,cached:true\}/);assert.match(card,/return\{files,cached:false\}/);assert.match(card,/sharePreparedCommission:shareFiles/);assert.match(card,/text:model\.caption\|\|model\.referralUrl/);assert.match(css,/\.commission-share-form/);
console.log('TikTok commission social workflow: PASS');
