import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(p,'utf8'),bot=r('functions/api/vision7/shops/[shopId]/bot.js');assert.equal(r('VERSION.txt').trim(),'v0.14.167');for(const x of[/running:state\?\.state==='running'/,/channel_readiness:Object\.fromEntries/,/row\?\.status==='connected'/,/lineReady/])assert.match(bot,x);console.log('PASS v0.14.167 LINE chat status refresh contract');
