import assert from "node:assert/strict";
import fs from "node:fs";

const read = file => fs.readFileSync(new URL(`../${file}`, import.meta.url), "utf8");
const client = read("public/tiktok-analyzer.js"), css = read("public/tiktok-analyzer.css"), html = read("public/tiktok-analyzer.html");

assert.match(html, /<h3 id="marketplaceTitle">ค้นหาสินค้านางฟ้า<\/h3>/);
assert.doesNotMatch(html, /ค้นหา Open Collaboration Marketplace/);
assert.doesNotMatch(html, /ผลค้นหาเป็นข้อมูลจาก TikTok ณ เวลาที่กดค้นหา/);
assert.doesNotMatch(html, /ผลค้นหาเป็นข้อมูลจาก TikTok ณ เวลาที่กดค้นหา/);
assert.match(client, /id="marketplaceShopPanel" class="marketplace-panel shop-search-panel"/);
assert.match(client, /<h3>ค้นหาสินค้าจากร้านค้า<\/h3>/);
assert.match(client, /โดยไม่กระทบผลค้นหาสินค้านางฟ้า/);
assert.match(client, /id="marketplaceShopSearchForm"/);
assert.match(client, /id="marketplaceShopSnapshot"/);
assert.match(client, /id="marketplaceShopResults"/);
assert.match(client, /id="addMarketplaceShopSelected"/);
assert.doesNotMatch(client, /กรองจากผล Marketplace ที่ TikTok ส่งมา/);
assert.match(css, /\.marketplace-shop-search-note\{/);
assert.match(client, /marketplaceProductButton\.textContent = "ค้นหาสินค้า"/);
assert.match(client, /searchMarketplace\("shop"\)/);
assert.doesNotMatch(client, /marketplaceSearchMode/);
assert.doesNotMatch(client, /event\.submitter\?\.dataset\.searchMode/);
assert.match(client, /shopMode \? \{ connection_id:[\s\S]*keyword: "", shop_keyword:/);
assert.match(client, /shop_keyword: \$\("#marketplaceShopKeyword"\)\.value\.trim\(\)/);
assert.match(client, /result_limit: 200/);
assert.match(client, /shopMarketplaceProducts/);
assert.match(client, /marketplaceView\(mode = "product"\)/);
assert.match(client, /view\.box\.querySelectorAll\("\.marketplace-product-check:checked"\)/);
assert.match(client, /data-add-marketplace-product=/);
assert.match(client, />เพิ่มเข้า Showcase<\/button>/);
assert.match(client, /addProductsToShowcase\(\[button\.dataset\.addMarketplaceProduct\], button, mode\)/);
assert.doesNotMatch(client, /ตรวจสิทธิ์ก่อนเพิ่มสินค้าเข้า Showcase/);
assert.match(client, /ต้องเปิด creator\.showcase\.write หรือ creator\.video\.write ใน TikTok Shop Partner Center/);
assert.match(css, /\.shop-search-panel\{margin-bottom:18px/);
assert.match(css, /\.marketplace-row-add\{/);
assert.match(html, /tiktok-analyzer\.js\?v=02078/);
assert.match(html, /tiktok-analyzer\.css\?v=02076/);

console.log("TikTok Marketplace separated product/shop search: PASS");
