import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js');assert.match(provider,/daily_product_list \(array of \{product\}\)/);assert.match(provider,/ไม่ใช่แผนคลิป/);assert.doesNotMatch(provider,/oneDayPostingPlanPrompt/);assert.match(html,/รายการสินค้าเตรียมทำคลิปวันนี้/);assert.match(html,/ไม่ใช่ตารางเวลาและไม่ใช่แผนคลิป/);assert.match(client,/result\.daily_product_list/);assert.match(client,/\.slice\(0,30\)/);assert.doesNotMatch(client,/CTA:|pass_condition/);assert.equal(read('VERSION.txt').trim(),'v0.14.513');console.log('TikTok daily product preparation list regression: PASS');
