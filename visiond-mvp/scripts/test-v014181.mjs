import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const r=p=>readFile(new URL('../'+p,import.meta.url),'utf8');const[version,index,admin,ledger]=await Promise.all([r('VERSION.txt'),r('public/index.html'),r('public/admin.html'),r('patch-ledgers/v0.14.181.json')]);assert.equal(version.trim(),'v0.14.181');assert.match(index,/WEB v0\.14\.181/);assert.match(admin,/ADMIN v0\.14\.181/);assert.match(index,/<head>[\s\S]*<body[\s\S]*<header[\s\S]*<main[\s\S]*<\/body>[\s\S]*<\/html>/);assert.equal(JSON.parse(ledger).patch,'v0.14.181');console.log('v0.14.181 minimal production visible version: PASS');
