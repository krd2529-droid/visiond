import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(provider,/audience_demographics \(\{primary_gender,primary_age_group,gender_breakdown: array of \{label,percentage\},age_breakdown: array of \{label,percentage\},evidence\}\)/);assert.match(provider,/ห้ามเดาจากเนื้อหาคลิป/);assert.match(html,/ฐานผู้ติดตาม: กลุ่มอายุและเพศ/);assert.doesNotMatch(html,/สูตรคลิปที่ควรทำซ้ำ/);assert.match(client,/result\.audience_demographics/);assert.match(client,/primary_gender/);assert.match(client,/primary_age_group/);assert.match(client,/demographic-pending/);assert.match(css,/\.audience-demographics/);assert.equal(read('VERSION.txt').trim(),'v0.14.516');console.log('TikTok follower demographics card regression: PASS');
