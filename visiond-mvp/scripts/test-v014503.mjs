import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html');assert.doesNotMatch(html,/class="steps"/);assert.doesNotMatch(html,/1 ใส่ช่องและภาพ/);assert.match(html,/1 ตั้งค่าช่องและเชื่อมข้อมูล/);assert.match(html,/2 ผลวิเคราะห์และ Dashboard/);assert.equal(read('VERSION.txt').trim(),'v0.14.503');console.log('TikTok redundant four-step strip removed: PASS');
