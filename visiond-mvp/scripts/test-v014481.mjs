import fs from'node:fs';import assert from'node:assert/strict';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),api=read('functions/api/admin/tiktok-analyzer/index.js'),provider=read('functions/_tiktok_analyzer.js');assert.match(provider,/A=ขายดีอยู่แล้ว/);assert.match(provider,/F=ไม่ควรยุ่ง/);assert.match(provider,/ประเภท F ต้องมีหลักฐานหรือความเสี่ยงชัด/);assert.match(provider,/avoid_products/);assert.match(api,/productType/);assert.match(api,/source_kind/);assert.match(api,/result\.avoid_products/);assert.match(client,/typeLabels=\{A:/);assert.match(client,/'ABCDEF'\.split/);assert.match(html,/A ขายดี · B ใหม่\/ใกล้เคียง/);assert.equal(read('VERSION.txt').trim(),'v0.14.481');console.log('v0.14.481 TikTok product formula A-F: PASS');
