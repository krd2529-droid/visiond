import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(client,/pending-chart-preview/);assert.match(client,/pending-channel-preview/);assert.match(client,/ตัวอย่างหน้าตาผลลัพธ์/);assert.match(css,/ตัวอย่างคาดการณ์ · รอ API/);assert.match(css,/pending-chart-preview/);assert.match(css,/pending-channel-preview/);assert.equal(read('VERSION.txt').trim(),'v0.14.512');console.log('TikTok API-pending illustrative preview regression: PASS');
