import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js');assert.match(client,/if\(!shopConnection&&!commission\.length\)\{renderPendingShopDashboard\(\)/);assert.match(client,/reviewDemoNotice'\)\.hidden=true/);assert.match(client,/renderLiveShopDashboard\(data,shopConnection\)/);assert.equal(read('VERSION.txt').trim(),'v0.14.499');console.log('TikTok live pending-layout parity regression: PASS');
