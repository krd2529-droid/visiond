import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(client,/Array\.from\(\{length:5\}/);assert.match(client,/ช่องตัวอย่าง/);assert.match(client,/demo-channel/);assert.match(client,/disabled/);assert.match(css,/demo-channel:disabled/);assert.equal(read('VERSION.txt').trim(),'v0.14.497');console.log('TikTok five demo channels regression: PASS');
