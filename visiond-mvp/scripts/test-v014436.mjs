import fs from 'node:fs';import assert from 'node:assert/strict';const read=file=>fs.readFileSync(new URL(`../${file}`,import.meta.url),'utf8'),html=read('public/admin-courses.html'),ui=read('public/admin-courses.js');
assert.equal(read('VERSION.txt').trim(),'v0.14.436');assert.match(read('public/index.html'),/WEB v0\.14\.436/);assert.match(read('public/admin.html'),/ADMIN v0\.14\.436/);assert.match(html,/admin-courses\.js\?v=014436/);assert.doesNotMatch(html,/กลับไปสร้างคอร์สใหม่/);assert.doesNotMatch(html,/id="closeLessons"/);assert.doesNotMatch(ui,/closeLessons/);assert.match(html,/id="lessonManager"/);assert.match(html,/id="publishCompanyCourse"/);console.log('PASS v0.14.436 remove company course reset button without dangling handler');
