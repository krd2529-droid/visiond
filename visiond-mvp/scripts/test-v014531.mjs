import assert from 'node:assert/strict';import fs from 'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js');assert.match(client,/normalizeProductName=value=>/);assert.match(client,/normalize\('NFKC'\)/);assert.match(client,/\\u200B-\\u200D\\uFEFF/);assert.match(client,/\[\^\\p\{L\}\\p\{N\}\]\+/);assert.match(client,/const name=normalizeProductName/);assert.match(client,/normalizeProductName\(x\.name\)/);assert.equal(read('VERSION.txt').trim(),'v0.14.531');console.log('TikTok robust duplicate product normalization: PASS');
