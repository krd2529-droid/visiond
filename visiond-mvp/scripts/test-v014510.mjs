import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),css=read('public/tiktok-analyzer.css');assert.equal((html.match(/duplicate-product-analysis/g)||[]).length,2);assert.match(css,/\.duplicate-product-analysis/);assert.match(css,/output-channel #angelInventory/);assert.match(html,/id="shopGradeList"/);assert.match(html,/id="removeShowcaseF"/);assert.equal(read('VERSION.txt').trim(),'v0.14.510');console.log('TikTok single actionable Showcase grade table regression: PASS');
