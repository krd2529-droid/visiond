import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');assert.equal(r('VERSION.txt').trim(),'v0.14.346');assert.match(r('public/index.html'),/WEB v0\.14\.346/);assert.match(r('public/admin.html'),/ADMIN v0\.14\.346/);const m=r('FEATURE-MAP.md'),ui=r('public/vision4-edit.html'),edit=r('public/vision4-edit.js'),review=r('functions/api/admin/vision4-review/[id].js'),consume=r('functions/api/admin/vision4-pending/consume.js'),complete=r('functions/api/admin/product-multipart/complete.js');for(const x of['V4-REVIEW-001','vision4_pending_files','waiting_bundle','PDF/ZIP ไม่เกิน 1 GB','data-feature="V4-REVIEW-001"'])assert.ok(m.includes(x),x);assert.match(ui,/data-feature="V4-REVIEW-001"/);assert.match(edit,/item\.source!=='vision4'\|\|item\.status!=='draft'/);assert.match(review,/SET source='admin'/);assert.match(consume,/status='published'/);assert.match(consume,/length!==ids\.length/);assert.match(complete,/size > 1024 \* 1024 \* 1024/);console.log('PASS v0.14.346 Vision 4 Review Feature Map');
