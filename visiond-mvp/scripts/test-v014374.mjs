import fs from'node:fs';import assert from'node:assert/strict';const r=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');assert.equal(r('VERSION.txt').trim(),'v0.14.374');assert.match(r('public/index.html'),/WEB v0\.14\.374/);assert.match(r('public/admin.html'),/ADMIN v0\.14\.374/);const ui=r('public/facebook-chat.js'),m=r('FEATURE-MAP.md');assert.match(ui,/root\.dataset\.feature='CONTACT-DOCK-001'/);for(const x of['CONTACT-DOCK-001','external contact launcher','noopener noreferrer','visiond_facebook_chat_open','560px','ไม่มี API call'])assert.ok(m.includes(x),x);assert.match(ui,/window\.VISIOND_FACEBOOK_PAGE_URL\|\|/);assert.match(ui,/window\.VISIOND_LINE_URL\|\|/);assert.match(ui,/target="_blank" rel="noopener noreferrer"/);assert.match(ui,/if\(document\.querySelector\('\[data-visiond-contact-dock\]'\)\)return/);assert.match(ui,/localStorage\.setItem\(STORAGE_KEY,open\?'1':'0'\)/);assert.match(ui,/launcher\.setAttribute\('aria-expanded',String\(open\)\)/);assert.match(ui,/panel\.hidden=!open/);assert.match(ui,/@media\(max-width:560px\)/);assert.doesNotMatch(ui,/fetch\(|\/api\//);console.log('PASS v0.14.374 External Contact Dock Feature Map');
