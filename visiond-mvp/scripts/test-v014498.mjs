import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),client=read('public/tiktok-analyzer.js');assert.match(client,/demoShopData=\{shop_portfolio:\{commission:\[\]\}/);assert.match(client,/<b>ช่อง \$\{index\+1\}<\/b>/);assert.doesNotMatch(client,/ช่อง A|ช่อง B|ช่อง C|ช่อง D|ช่องตัวอย่าง/);assert.equal(read('VERSION.txt').trim(),'v0.14.498');console.log('TikTok numeric demo channel labels regression: PASS');
