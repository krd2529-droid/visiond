import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(html,/วิเคราะห์ภาพรวมทุกช่อง/);assert.match(html,/วิเคราะห์ทีละช่อง/);assert.match(html,/channelOutputEmpty/);assert.match(client,/setOutputScope/);assert.match(css,/output-overview #result/);assert.match(css,/output-channel #shopDashboard/);assert.equal(read('VERSION.txt').trim(),'v0.14.502');console.log('TikTok overview and per-channel output split regression: PASS');
