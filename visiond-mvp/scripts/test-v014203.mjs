import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const read=p=>readFile(new URL(`../${p}`,import.meta.url),'utf8');const[v,i,a,h,ui,api]=await Promise.all([read('VERSION.txt'),read('public/index.html'),read('public/admin.html'),read('public/v12-connect.html'),read('public/v12-connect.js'),read('functions/api/admin/v12-broadcast.js')]);assert.equal(v.trim(),'v0.14.203');assert.match(i,/WEB v0\.14\.203/);assert.match(a,/ADMIN v0\.14\.203/);for(const x of['v12RecipientPicker','v12ConversationIds','v12RecipientSummary'])assert.ok(h.includes(x),x);for(const x of['syncRecipientPicker','recipientSignature','testedRecipientSignature'])assert.ok(ui.includes(x),x);assert.match(api,/recipients/);assert.match(api,/display_name/);console.log('v0.14.203 broadcast recipient picker: PASS');
