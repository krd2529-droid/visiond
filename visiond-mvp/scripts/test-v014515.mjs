import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(provider,/clipsPerDay=40/);assert.match(provider,/30 อันแรกเป็นชุดหลักและ 10 อันท้ายเป็นตัวสำรอง/);assert.match(html,/40 สินค้า \(30 หลัก \+ 10 สำรอง\)/);assert.match(html,/ลิสต์สินค้าสำหรับคัดเลือก 40 รายการ/);assert.match(client,/\.slice\(0,40\)/);assert.match(client,/dailyProducts\.length\}\/40/);assert.match(client,/backup=index>=30/);assert.match(css,/\.product-prep-item\.backup/);assert.equal(read('VERSION.txt').trim(),'v0.14.515');console.log('TikTok 40-product shortlist regression: PASS');
