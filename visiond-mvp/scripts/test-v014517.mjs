import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(provider,/daily_product_list \(array of \{rank,product,product_type,ranking_score,ranking_reason\}\)/);assert.match(provider,/ranking_score 0-100/);assert.match(provider,/ห้ามแต่งคะแนน/);assert.match(html,/Ranking สินค้า 40 รายการ/);assert.match(client,/ranking_score/);assert.match(client,/ranking_reason/);assert.match(client,/Number\(x\?\.rank\)\|\|index\+1/);assert.match(client,/กดวิเคราะห์ใหม่เพื่อสร้าง Ranking/);assert.match(css,/\.ranking-score/);assert.equal(read('VERSION.txt').trim(),'v0.14.517');console.log('TikTok product ranking regression: PASS');
