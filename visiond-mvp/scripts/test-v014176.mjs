import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const r=p=>readFile(new URL('../'+p,import.meta.url),'utf8');const[version,index,admin,dashboard,runtime,mobile]=await Promise.all([r('VERSION.txt'),r('public/index.html'),r('public/admin.html'),r('public/dashboard.html'),r('public/build-version.js'),r('public/mobile-storefront.js')]);for(const text of[index,admin,dashboard,runtime])assert.match(text,/v0\.14\.176/);assert.equal(version.trim(),'v0.14.176');assert.match(mobile,/build-version\.js\?v=014176/);for(const stale of['v0.14.169','v0.14.170'])for(const text of[index,admin,dashboard])assert.doesNotMatch(text,new RegExp(stale.replaceAll('.','\\.')));console.log('v0.14.176 centralized visible version: PASS');
