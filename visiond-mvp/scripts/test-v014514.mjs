import assert from'node:assert/strict';import fs from'node:fs';const read=file=>fs.readFileSync(new URL('../'+file,import.meta.url),'utf8'),provider=read('functions/_tiktok_analyzer.js'),html=read('public/tiktok-analyzer.html'),client=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(provider,/daily_product_list \(array of \{product,product_type\}\)/);assert.match(provider,/product_type ซึ่งต้องเป็น A, B, C, D หรือ E/);assert.match(html,/id="productPrepSummary"/);assert.match(client,/รวม <b>\$\{dailyProducts\.length\}\/30<\/b> สินค้า/);assert.match(client,/gradeCounts=\{A:0,B:0,C:0,D:0,E:0,unknown:0\}/);assert.match(client,/ยังไม่ระบุเกรด/);assert.match(css,/\.product-prep-summary/);assert.match(css,/\.product-prep-grade/);assert.equal(read('VERSION.txt').trim(),'v0.14.514');console.log('TikTok daily product grade summary regression: PASS');
