import assert from'node:assert/strict';import fs from'node:fs';const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),'utf8'),ui=read('public/tiktok-analyzer.js'),css=read('public/tiktok-analyzer.css');assert.match(ui,/id="commissionImagePreview"/);assert.match(ui,/commissionPreviewUrls/);assert.match(ui,/URL\.revokeObjectURL/);assert.match(ui,/prepared\.files\.map\(file=>URL\.createObjectURL\(file\)\)/);assert.match(ui,/รูปสรุปค่าคอม หน้า/);assert.match(ui,/preview\.hidden=false/);assert.match(css,/\.commission-image-preview/);assert.match(css,/\.commission-image-preview\[hidden\]/);console.log('TikTok commission image preview: PASS');
