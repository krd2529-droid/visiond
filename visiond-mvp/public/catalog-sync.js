(()=>{const grid=document.querySelector('.vd-grid');if(!grid)return;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=n=>new Intl.NumberFormat('th-TH').format((Number(n)||0)/100)+' บาท';fetch('/api/products').then(r=>r.ok?r.json():Promise.reject()).then(data=>{const existing=new Set([...grid.querySelectorAll('a[href*="slug="]')].map(a=>new URL(a.href,location.origin).searchParams.get('slug')));const additions=(data.items||[]).filter(p=>!existing.has(p.slug));grid.insertAdjacentHTML('beforeend',additions.map(p=>`<article class="vd-card"><a class="vd-cover" href="/product.html?slug=${encodeURIComponent(p.slug)}"><span class="vd-tag">DIGITAL</span><span class="vd-ready">พร้อมดาวน์โหลด</span><img src="${esc(p.cover_url||'/assets/product-placeholder.svg')}" alt="${esc(p.title)}"></a><div class="vd-info"><small>VD-${String(p.id).padStart(3,'0')}</small><h2><a href="/product.html?slug=${encodeURIComponent(p.slug)}">${esc(p.title)}</a></h2><p>${esc(p.short_description||p.category||'สินค้าดิจิทัล')}</p><div class="vd-bottom"><b>${money(p.price)}</b><a href="/product.html?slug=${encodeURIComponent(p.slug)}">ดูสินค้า</a></div></div></article>`).join(''))}).catch(()=>{})})();
