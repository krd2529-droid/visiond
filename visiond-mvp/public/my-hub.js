const hubEsc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const hubMoney=n=>new Intl.NumberFormat('th-TH').format((Number(n)||0)/100)+' บาท';
async function dismissNotice(key){const r=await fetch('/api/notifications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({key})});if(r.ok)loadMyHub()}
async function loadMyHub(){try{
  const [sr,cr,nr]=await Promise.all([fetch('/api/course-seller',{cache:'no-store'}),fetch('/api/courses',{cache:'no-store'}),fetch('/api/notifications',{cache:'no-store'})]),seller=sr.ok?await sr.json():{},courses=cr.ok?await cr.json():{},notices=nr.ok?await nr.json():{items:[],count:0},credits=Number(seller.credit_balance)||0;
  hubDrafts.textContent=`${Number(seller.course_draft_count)||0}/${Number(seller.course_draft_limit)||3}`;hubDrafts.nextElementSibling.textContent='สร้างคอร์ส';hubCredits.textContent=credits;hubPendingSlips.textContent=(seller.slip_issues||[]).length;hubSales.textContent=hubMoney(seller.totals?.amount);
  const created=seller.courses||[];hubCreatedCourses.innerHTML=created.length?created.map(c=>{const bound=c.license_entitlement_id!==null,publishHref=credits>0?`/course-seller.html?create=1&course_id=${c.id}`:'/product.html?slug=course-selling-rights',publishLabel=credits>0?'เผยแพร่':'เผยแพร่ (เติมเครดิตก่อน)';return `<article class="hub-row hub-course-row"><img src="${hubEsc(c.cover_url)}" alt=""><div><b>${hubEsc(c.title)}</b><span>${Number(c.lesson_count)||0}/${Number(c.expected_episodes)||1} EP · ${bound?'ผูกตะกร้าแล้ว':'ยังไม่ผูกตะกร้า'}</span><div class="hub-course-actions"><a class="hub-draft" href="/course-seller.html?course_id=${c.id}">บันทึกร่าง</a>${bound?'<span class="hub-published">ผูกตะกร้าแล้ว</span>':`<a class="hub-publish" href="${publishHref}">${publishLabel}</a>`}</div></div></article>`}).join(''):'<p>ยังไม่มีคอร์ส สร้างฟรีได้สูงสุด 3 คอร์ส</p>';
  const learning=(courses.items||[]).filter(x=>x.owned);hubLearningCourses.innerHTML=learning.length?learning.map(c=>`<a class="hub-row" href="/learn.html?course=${c.id}"><img src="${hubEsc(c.cover_url)}" alt=""><div><b>${hubEsc(c.title)}</b><span>เรียนแล้ว ${Number(c.completed_lessons)||0}/${Number(c.lesson_count)||0} บท</span></div><strong>เรียนต่อ</strong></a>`).join(''):'<p>ยังไม่มีคอร์สเรียน</p>';
  const baskets=created.filter(c=>c.license_entitlement_id!==null);hubBaskets.innerHTML=baskets.length?baskets.map(c=>`<article class="hub-row"><div><b>${hubEsc(c.title)}</b><span>${hubEsc(c.status||'draft')}</span></div></article>`).join(''):'<p>ยังไม่มีตะกร้าที่ผูกคอร์ส</p>';hubSalesPanel.innerHTML=`<p>ยอดขายรวม <b>${hubMoney(seller.totals?.amount)}</b> · ออเดอร์สำเร็จ <b>${Number(seller.totals?.orders)||0}</b></p>`;hubRights.innerHTML=`<p>เครดิตตะกร้าคงเหลือ <b>${credits}</b> แต้ม</p><p>คอร์สที่ยังไม่ผูก ${created.filter(c=>c.license_entitlement_id===null).length} คอร์ส</p>`;
  const noticeCount=Number(notices.count)||0;hubNotificationCount.textContent=noticeCount;const noticeButton=document.querySelector('[data-dash="notifications"]');if(noticeButton)noticeButton.textContent=`9. การแจ้งเตือน${noticeCount?` (${noticeCount})`:''}`;const items=notices.items||[];hubNotifications.innerHTML=items.length?items.map(n=>`<article class="hub-notice ${hubEsc(n.type)}"><div><b>${hubEsc(n.title)}</b><p>${hubEsc(n.message)}</p></div><div class="hub-notice-actions"><a href="${hubEsc(n.href||'/dashboard.html')}">ไปจัดการ</a><button type="button" data-notice-key="${hubEsc(n.key)}">รับทราบ</button></div></article>`).join(''):'<p>ไม่มีรายการที่ต้องดำเนินการ</p>';hubNotifications.querySelectorAll('[data-notice-key]').forEach(b=>b.onclick=()=>dismissNotice(b.dataset.noticeKey));
}catch(e){console.warn('my hub unavailable',e)}}
loadMyHub();
