const message=document.getElementById('pageAuthMsg');const show=t=>{if(message)message.textContent=t};
async function postForm(form,url){const button=form.querySelector('button[type="submit"]');button.disabled=true;show('กำลังดำเนินการ…');try{const data=Object.fromEntries(new FormData(form));if(data.confirmPassword!==undefined&&data.password!==data.confirmPassword)throw new Error('รหัสผ่านทั้งสองช่องไม่ตรงกัน');delete data.confirmPassword;delete data.remember;const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'ทำรายการไม่สำเร็จ');location.href='/dashboard.html'}catch(e){show(e.message)}finally{button.disabled=false}}
const login=document.getElementById('loginPageForm');if(login)login.onsubmit=e=>{e.preventDefault();postForm(login,'/api/auth/login')};
const register=document.getElementById('registerPageForm');if(register)register.onsubmit=e=>{e.preventDefault();postForm(register,'/api/auth/register')};
const forgot=document.getElementById('forgotForm');if(forgot)forgot.onsubmit=e=>{e.preventDefault();show('ระบบกู้รหัสผ่านจะเปิดใช้งานหลังเชื่อมบริการอีเมล กรุณาติดต่อ LINE ในช่วงนี้')};
