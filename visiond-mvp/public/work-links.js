const $=selector=>document.querySelector(selector),form=$('#linkForm'),status=$('#linkStatus'),list=$('#linkList'),search=$('#linkSearch');
let items=[],editingId=null;
function setStatus(message,error=false){status.textContent=message;status.classList.toggle('error',error)}
async function request(options={}){const response=await fetch('/api/admin/work-links',options),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'ทำรายการไม่สำเร็จ');return data}
function reset(){editingId=null;form.reset();$('#formTitle').textContent='เพิ่มลิงก์งาน';$('#saveLink').textContent='เพิ่มลิงก์';$('#cancelEdit').hidden=true}
function render(){const query=search.value.trim().toLowerCase(),shown=items.filter(item=>`${item.label} ${item.note} ${item.url}`.toLowerCase().includes(query));$('#linkCount').textContent=`${items.length} รายการ`;list.replaceChildren();if(!shown.length){const empty=document.createElement('p');empty.className='empty';empty.textContent=query?'ไม่พบลิงก์ที่ค้นหา':'ยังไม่มีลิงก์งาน';list.append(empty);return}for(const item of shown){const row=document.createElement('article');row.className='link-row';const logo=document.createElement('span'),image=document.createElement('img');logo.className='link-logo';logo.textContent='🔗';image.alt='';image.loading='lazy';image.src=`${new URL(item.url).origin}/favicon.ico`;image.addEventListener('load',()=>{logo.textContent='';logo.append(image)});const info=document.createElement('div'),title=document.createElement('h3'),note=document.createElement('p'),time=document.createElement('small');title.textContent=item.label;note.textContent=item.note||item.url;time.textContent=`แก้ไขล่าสุด ${new Date(item.updated_at+'Z').toLocaleString('th-TH')}`;info.append(title,note,time);const actions=document.createElement('div');actions.className='link-actions';const open=document.createElement('a');open.className='vds-btn vds-btn--primary vds-btn--small';open.href=item.url;open.target='_blank';open.rel='noopener noreferrer';open.textContent='เปิดลิงก์';const edit=document.createElement('button');edit.type='button';edit.className='vds-btn vds-btn--secondary vds-btn--small';edit.textContent='แก้ไข';edit.onclick=()=>{editingId=item.id;$('#linkLabel').value=item.label;$('#linkUrl').value=item.url;$('#linkNote').value=item.note;$('#formTitle').textContent='แก้ไขลิงก์งาน';$('#saveLink').textContent='บันทึก';$('#cancelEdit').hidden=false;form.scrollIntoView({behavior:'smooth'});$('#linkLabel').focus()};const remove=document.createElement('button');remove.type='button';remove.className='vds-btn vds-btn--danger vds-btn--small';remove.textContent='ลบ';remove.onclick=()=>deleteItem(item,remove);actions.append(open,edit,remove);row.append(logo,info,actions);list.append(row)}}
async function load(){setStatus('กำลังโหลด…');try{const data=await request();items=data.items;render();setStatus('พร้อมใช้งาน')}catch(error){setStatus(error.message,true)}}
async function deleteItem(item,button){if(!confirm(`ลบลิงก์ “${item.label}” ใช่ไหม?`))return;button.disabled=true;try{const data=await request({method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({id:item.id})});items=data.items;render();if(editingId===item.id)reset();setStatus('ลบแล้ว')}catch(error){setStatus(error.message,true);button.disabled=false}}
form.addEventListener('submit',async event=>{event.preventDefault();const button=$('#saveLink'),wasEditing=Boolean(editingId);button.disabled=true;try{const body={id:editingId,label:$('#linkLabel').value,url:$('#linkUrl').value,note:$('#linkNote').value},data=await request({method:wasEditing?'PATCH':'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});items=data.items;render();reset();setStatus(wasEditing?'บันทึกการแก้ไขแล้ว':'เพิ่มลิงก์แล้ว')}catch(error){setStatus(error.message,true)}finally{button.disabled=false}});
$('#cancelEdit').addEventListener('click',reset);search.addEventListener('input',render);load();
