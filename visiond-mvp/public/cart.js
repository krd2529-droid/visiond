const money=n=>new Intl.NumberFormat('th-TH').format((Number(n)||0)/100)+' บาท';
const getCart=()=>{try{return JSON.parse(localStorage.getItem('vd_cart')||'[]')}catch{return[]}};
const saveCart=items=>{localStorage.setItem('vd_cart',JSON.stringify(items));render()};
function render(){const items=getCart();document.querySelectorAll('[data-cart-count]').forEach(x=>x.textContent=items.length);cartQty.textContent=items.length+' รายการ';cartTotal.textContent=money(items.reduce((sum,x)=>sum+Number(x.price||0),0));cartItems.innerHTML=items.length?items.map((p,i)=>`<article class="cart-product"><img src="${p.cover_url||'/assets/product-placeholder.svg'}" alt=""><div><small>${p.category_label||p.category||'ไฟล์ระบายสี'}</small><h2>${p.title}</h2><p>ไฟล์ PDF พร้อมพิมพ์ • ${p.pages||'-'} หน้า</p><a href="/product.html?slug=${encodeURIComponent(p.slug)}">ดูรายละเอียด</a></div><div class="cart-product-price"><b>${money(p.price)}</b><button data-remove="${i}" type="button">นำออก</button></div></article>`).join(''):`<div class="cart-empty"><b>ตะกร้ายังว่าง</b><p>เลือกไฟล์ระบายสีที่ชอบ แล้วเพิ่มลงตะกร้าได้เลย</p><a class="primary" href="/digital-products.html">เลือกดูสินค้า</a></div>`;document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>saveCart(items.filter((_,i)=>i!==Number(b.dataset.remove))));checkoutButton.disabled=!items.length}
checkoutButton.onclick=()=>{if(!getCart().length)return;sessionStorage.setItem('vd_return_to','/cart.html');location.href='/login.html'};render();
