const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const form=document.getElementById('sourceForm'),state=document.getElementById('sourceState'),list=document.getElementById('sourceList'),reload=document.getElementById('reloadSources');
let rights=[];
const size=value=>new Intl.NumberFormat('th-TH',{maximumFractionDigits:1}).format((Number(value)||0)/1024/1024)+' MB';
const options=value=>rights.map(item=>`<option value="${esc(item.value)}" ${item.value===value?'selected':''}>${esc(item.label)}</option>`).join('');
function card(item){return `<article class="source-card" data-id="${esc(item.id)}"><div class="source-head"><div><h3>${esc(item.title)}</h3><small>${esc(item.original_file_name)} · ${size(item.file_size)} · รับโดย ${esc(item.created_by_name||'-')}</small></div><b class="${item.sale_eligible?'eligible':'blocked'}">${item.sale_eligible?'สิทธิ์ผ่านด่านขายเบื้องต้น':'ล็อกการขาย'}</b></div><p>สถานะ: ${esc(item.processing_status)}</p><div class="source-actions"><select aria-label="สถานะสิทธิ์">${options(item.rights_status)}</select><input aria-label="หมายเหตุสิทธิ์" maxlength="500" value="${esc(item.rights_note||'')}" placeholder="หมายเหตุ/หลักฐานสิทธิ์"><button type="button">บันทึกสิทธิ์</button><span role="status"></span></div></article>`}
async function load(){reload.disabled=true;list.textContent='กำลังโหลด…';const response=await fetch('/api/admin/vision14-sources',{cache:'no-store'}),data=await response.json().catch(()=>({}));reload.disabled=false;if(!response.ok){list.innerHTML=`<p class="error">${esc(data.error||'โหลดคลังไม่สำเร็จ')}</p>`;return}rights=data.rights||[];list.innerHTML=data.items.length?data.items.map(card).join(''):'<p>ยังไม่มี PDF ต้นฉบับ</p>';list.querySelectorAll('.source-card').forEach(element=>{const button=element.querySelector('button'),message=element.querySelector('[role=status]');button.onclick=async()=>{button.disabled=true;message.textContent='กำลังบันทึก…';const response=await fetch(`/api/admin/vision14-sources/${element.dataset.id}`,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({rights_status:element.querySelector('select').value,rights_note:element.querySelector('input').value})}),data=await response.json().catch(()=>({}));button.disabled=false;if(!response.ok){message.className='error';message.textContent=data.error||'บันทึกไม่สำเร็จ';return}message.className='';message.textContent='บันทึกแล้ว';await load()}})}
form.onsubmit=async event=>{event.preventDefault();const button=form.querySelector('button'),file=form.elements.file.files[0];state.className='';if(!file||file.size>100*1024*1024){state.className='error';state.textContent='เลือก PDF ขนาดไม่เกิน 100 MB';return}button.disabled=true;state.textContent='กำลังรับ PDF เข้าคลัง…';const response=await fetch('/api/admin/vision14-sources',{method:'POST',body:new FormData(form)}),data=await response.json().catch(()=>({}));button.disabled=false;if(!response.ok){state.className='error';state.textContent=data.error||'รับไฟล์ไม่สำเร็จ';return}state.textContent=data.message;form.reset();form.elements.rights_status.value='reference_only';await load()};reload.onclick=load;load();
