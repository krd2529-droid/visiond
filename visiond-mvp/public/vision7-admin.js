const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),dlg=document.querySelector('#programDialog');async function load(){const [p,l]=await Promise.all([fetch('/api/admin/vision7/programs'),fetch('/api/admin/vision7/licenses')]),pd=await p.json(),ld=await l.json();if(p.status===401||p.status===403){location.href='/login.html?next=/vision7-admin.html';return}document.querySelector('#programs').innerHTML=(pd.items||[]).map(x=>`<p><b>${esc(x.product_title||x.code)}</b><br>${esc(x.code)} · v${esc(x.current_version)} · 3 เครื่อง</p>`).join('')||'ยังไม่มีโปรแกรม';document.querySelector('#licenses').innerHTML=(ld.items||[]).slice(0,20).map(x=>`<p><b>${esc(x.user_name)}</b> · ${esc(x.program_title||x.program_code)}<br>${esc(x.key_masked)} · ${esc(x.status)} · ${x.active_devices}/3 เครื่อง</p>`).join('')||'ยังไม่มีคีย์'}document.querySelector('#newProgram').onclick=()=>dlg.showModal();document.querySelector('#newKey').onclick=()=>alert('การออกคีย์ต้องระบุ User ID, Program ID และ Plan ID ผ่าน API ในแพตฐานนี้ หน้าฟอร์มเต็มจะมาต่อแพตถัดไป');document.querySelector('#saveProgram').onclick=async e=>{e.preventDefault();const f=new FormData(dlg.querySelector('form')),b=Object.fromEntries(f);b.plans=[{plan_code:'lifetime',name:'ตลอดชีพ',price:0,product_id:b.lifetime_product_id},{plan_code:'monthly',name:'รายเดือน',price:0,product_id:b.monthly_product_id},{plan_code:'yearly',name:'รายปี',price:0,product_id:b.yearly_product_id}];const r=await fetch('/api/admin/vision7/programs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)}),d=await r.json();if(!r.ok)return alert(d.error||'บันทึกไม่สำเร็จ');dlg.close();load()};load();
