const money=n=>new Intl.NumberFormat('th-TH').format(n/100)+' บาท';async function load(){const r=await fetch('/api/admin/orders');const d=await r.json();if(!r.ok){adminOrders.innerHTML='<div class="admin-card"><b>เข้าไม่ได้</b><p>'+ (d.error||'กรุณาเข้าสู่ระบบด้วยบัญชีแอดมิน') +'</p><a class="primary" href="/account.html">เข้าสู่ระบบ</a></div>';return}adminOrders.innerHTML=(d.items||[]).map(o=>`<article class="admin-card"><div class="section-head"><div><b>${o.order_no}</b><p>${o.customer_name} · ${o.customer_email}</p></div><span class="status ${o.status}">${o.status_label}</span></div><p>${o.items.map(i=>i.title).join(', ')}</p><b>${money(o.total)}</b>${o.slip_url?`<p><a href="${o.slip_url}" target="_blank"><img class="slip-preview" src="${o.slip_url}"></a></p>`:'<p>ยังไม่มีสลิป</p>'}<div class="actions"><button class="primary" data-act="approve" data-id="${o.id}">อนุมัติและปลดล็อกไฟล์</button><button class="danger" data-act="reject" data-id="${o.id}">ปฏิเสธ</button></div></article>`).join('')||'<p>ไม่มีออเดอร์รอตรวจ</p>';document.querySelectorAll('[data-act]').forEach(b=>b.onclick=()=>act(b.dataset.id,b.dataset.act))}async function act(id,action){const note=prompt(action==='approve'?'หมายเหตุการอนุมัติ (ไม่บังคับ)':'เหตุผลที่ปฏิเสธ')||'';const r=await fetch('/api/admin/orders/'+id+'/'+action,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({note})});const d=await r.json();if(!r.ok)return alert(d.error);load()}load();
