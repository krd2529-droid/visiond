const money=n=>new Intl.NumberFormat('th-TH').format(n/100)+' บาท';let viewer=null;
const roleText={boss:'Boss',admin:'Admin',user:'User',customer:'User'};
document.querySelectorAll('[data-admin-tab]').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('[data-admin-tab]').forEach(x=>x.classList.toggle('active',x===btn));ordersPanel.hidden=btn.dataset.adminTab!=='orders';usersPanel.hidden=btn.dataset.adminTab!=='users';if(btn.dataset.adminTab==='users')loadUsers()});
async function init(){const me=await fetch('/api/auth/me');if(!me.ok){deny('กรุณาเข้าสู่ระบบก่อน');return}viewer=(await me.json()).user;adminIdentity.innerHTML=`เข้าสู่ระบบเป็น <b>${viewer.name}</b> · <span class="role-badge ${viewer.role}">${roleText[viewer.role]||viewer.role}</span>`;if(!['boss','admin'].includes(viewer.role)){deny('บัญชี User ไม่มีสิทธิ์เข้าหลังบ้าน');return}adminPanel.hidden=false;loadOrders()}
function deny(text){accessDenied.innerHTML=`<div class="admin-card"><b>เข้าไม่ได้</b><p>${text}</p><a class="primary" href="/account.html">เข้าสู่ระบบหรือกลับหน้าบัญชี</a></div>`}
async function loadOrders(){const r=await fetch('/api/admin/orders');const d=await r.json();if(!r.ok){deny(d.error||'ไม่มีสิทธิ์');return}adminOrders.innerHTML=(d.items||[]).map(o=>`<article class="admin-card"><div class="section-head"><div><b>${o.order_no}</b><p>${o.customer_name} · ${o.customer_email}</p></div><span class="status ${o.status}">${o.status_label}</span></div><p>${o.items.map(i=>i.title).join(', ')}</p><b>${money(o.total)}</b>${o.slip_url?`<p><a href="${o.slip_url}" target="_blank"><img class="slip-preview" src="${o.slip_url}"></a></p>`:'<p>ยังไม่มีสลิป</p>'}<div class="actions"><button class="primary" data-act="approve" data-id="${o.id}">อนุมัติและปลดล็อกไฟล์</button><button class="danger" data-act="reject" data-id="${o.id}">ปฏิเสธ</button></div></article>`).join('')||'<p>ไม่มีออเดอร์รอตรวจ</p>';document.querySelectorAll('[data-act]').forEach(b=>b.onclick=()=>act(b.dataset.id,b.dataset.act))}
async function act(id,action){const note=prompt(action==='approve'?'หมายเหตุการอนุมัติ (ไม่บังคับ)':'เหตุผลที่ปฏิเสธ')||'';const r=await fetch('/api/admin/orders/'+id+'/'+action,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({note})});const d=await r.json();if(!r.ok)return alert(d.error);loadOrders()}
async function loadUsers(){const r=await fetch('/api/admin/users');const d=await r.json();if(!r.ok){usersTable.innerHTML=`<p>${d.error||'โหลดสมาชิกไม่สำเร็จ'}</p>`;return}usersTable.innerHTML=`<div class="user-table"><div class="user-row user-head"><b>สมาชิก</b><b>ไอดี</b><b>ระดับ</b><b>จัดการ</b></div>${d.items.map(u=>`<div class="user-row"><div><b>${u.name}</b><small>${u.email}</small></div><span>${u.username||'-'}</span><span class="role-badge ${u.role}">${roleText[u.role]||u.role}</span><div>${viewer.role==='boss'&&u.role!=='boss'?`<select data-role-id="${u.id}"><option value="user" ${['user','customer'].includes(u.role)?'selected':''}>User</option><option value="admin" ${u.role==='admin'?'selected':''}>Admin</option></select><button data-save-role="${u.id}">บันทึก</button>`:'<span class="muted">ดูอย่างเดียว</span>'}</div></div>`).join('')}</div>`;document.querySelectorAll('[data-save-role]').forEach(b=>b.onclick=()=>saveRole(b.dataset.saveRole))}
async function saveRole(id){const select=document.querySelector(`[data-role-id="${id}"]`);if(!confirm(`เปลี่ยนระดับสมาชิกเป็น ${select.value.toUpperCase()} หรือไม่?`))return;const r=await fetch(`/api/admin/users/${id}/role`,{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({role:select.value})});const d=await r.json().catch(()=>({}));if(!r.ok)return alert(d.error||'บันทึกไม่สำเร็จ');loadUsers()}
init();
