const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
(async()=>{const me=await fetch('/api/auth/me');if(!me.ok){sessionStorage.setItem('vd_return_to','/my-courses.html');location.href='/login.html';return}const r=await fetch('/api/courses',{cache:'no-store'}),d=await r.json();const items=(d.items||[]).filter(x=>x.owned);myCourseList.innerHTML=items.length?items.map(c=>`<article class="course-card"><img src="${esc(c.cover_url||'/assets/product-placeholder.svg')}" alt="${esc(c.title)}"><div class="course-card-body"><small>${Number(c.lesson_count)||0} บท</small><h2>${esc(c.title)}</h2><progress max="${Math.max(1,Number(c.lesson_count)||1)}" value="${Number(c.completed_lessons)||0}"></progress><p>เรียนแล้ว ${Number(c.completed_lessons)||0}/${Number(c.lesson_count)||0} บท</p><a class="primary" href="/learn.html?course=${c.id}">${Number(c.completed_lessons)?'เรียนต่อ':'เริ่มเรียน'}</a></div></article>`).join(''):'<div class="course-empty"><b>ยังไม่มีคอร์สที่ได้รับอนุมัติ</b><p>เลือกซื้อคอร์ส แล้วส่งสลิปเพื่อรออนุมัติ</p><a class="primary" href="/courses.html">เลือกดูคอร์ส</a></div>'})();
