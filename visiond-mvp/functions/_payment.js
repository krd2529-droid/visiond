export async function ensureSettings(env){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS settings (key TEXT PRIMARY KEY,value TEXT NOT NULL DEFAULT '',updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run()}
export async function loadPaymentSettings(env){await ensureSettings(env);const {results}=await env.DB.prepare(`SELECT key,value FROM settings WHERE key IN ('bank_name','account_name','account_number','qr_url','accepting_orders','payment_message')`).all();const map=Object.fromEntries(results.map(x=>[x.key,x.value]));return {bank_name:map.bank_name||env.BANK_NAME||'ธนาคารกสิกรไทย',account_name:map.account_name||env.BANK_ACCOUNT_NAME||'บจก. วิชั่น ดี ออนไลน์',account_number:map.account_number||env.BANK_ACCOUNT_NUMBER||'209-2-90757-3',qr_url:map.qr_url||env.PAYMENT_QR_URL||'',accepting_orders:map.accepting_orders!=='0',payment_message:map.payment_message||'ส่งสลิปแล้ว กรุณารอแอดมินตรวจสอบและอนุมัติไฟล์'} }
export async function saveSetting(env,key,value){await ensureSettings(env);await env.DB.prepare(`INSERT INTO settings(key,value,updated_at) VALUES(?,?,CURRENT_TIMESTAMP) ON CONFLICT(key) DO UPDATE SET value=excluded.value,updated_at=CURRENT_TIMESTAMP`).bind(key,String(value??'')).run()}
