export async function onRequestGet(ctx){if(!String(ctx.params.key).startsWith('cover-'))return new Response('Not found',{status:404});const obj=await ctx.env.FILES.get(ctx.params.key);if(!obj)return new Response('Not found',{status:404});const headers=new Headers();obj.writeHttpMetadata(headers);headers.set('cache-control','public, max-age=86400');headers.set('etag',obj.httpEtag);return new Response(obj.body,{headers})}
