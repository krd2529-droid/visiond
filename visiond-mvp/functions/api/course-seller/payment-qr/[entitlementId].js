import { json,requireUser } from '../../../_lib.js';
import { ensureDatabase } from '../../../_schema.js';
export async function onRequestGet(ctx){await ensureDatabase(ctx.env);const auth=await requireUser(ctx);if(auth.error)return auth.error;const row=await ctx.env.DB.prepare(`SELECT u.seller_payment_qr_url FROM courses c JOIN users u ON u.id=c.owner_user_id WHERE c.license_entitlement_id=?`).bind(ctx.params.entitlementId).first();if(!row?.seller_payment_qr_url)return json({error:'ไม่พบ QR'},404);const object=await ctx.env.FILES.get(row.seller_payment_qr_url);if(!object)return json({error:'ไม่พบ QR'},404);const h=new Headers();object.writeHttpMetadata(h);h.set('cache-control','private, no-store');return new Response(object.body,{headers:h})}
