import {json,requireUser} from '../../../_lib.js';import {ensureDatabase} from '../../../_schema.js';
const extension=(name,type)=>String(name||'').split('.').pop()?.toLowerCase()||({'video/mp4':'mp4','video/webm':'webm','application/pdf':'pdf'}[type]||'bin');
async function owned(ctx,user){return ctx.env.DB.prepare("SELECT c.id,c.edit_expires_at,c.review_status FROM courses c JOIN products p ON p.id=c.product_id WHERE c.id=? AND c.owner_user_id=? AND c.course_origin='seller_rights' AND p.deleted_at IS NULL").bind(ctx.params.id,user.id).first()}
const editable=c=>!c.edit_expires_at||Date.parse(c.edit_expires_at)>Date.now();
export async function onRequestGet(ctx){await ensureDatabase(ctx.env);const auth=await requireUser(ctx);if(auth.error)return auth.error;const course=await owned(ctx,auth.user);if(!course)return json({error:'ไม่พบคอร์ส'},404);const rows=await ctx.env.DB.prepare('SELECT id,title,description,sort_order,duration_seconds,CASE WHEN video_key IS NULL THEN 0 ELSE 1 END has_video,CASE WHEN pdf_key IS NULL THEN 0 ELSE 1 END has_pdf FROM course_lessons WHERE course_id=? ORDER BY sort_order,id').bind(course.id).all();return json({items:rows.results||[],editable:editable(course)},200,{'cache-control':'no-store'})}
export async function onRequestPost(ctx){await ensureDatabase(ctx.env);const auth=await requireUser(ctx);if(auth.error)return auth.error;const course=await owned(ctx,auth.user);if(!course)return json({error:'ไม่พบคอร์ส'},404);if(!editable(course))return json({error:'หมดระยะเวลาแก้ไข 30 วันแล้ว'},403);const form=await ctx.request.formData(),title=String(form.get('title')||'').trim();if(!title)return json({error:'กรุณาใส่ชื่อบทเรียน'},400);const video=form.get('video'),pdf=form.get('pdf');if(!video?.size&&!pdf?.size)return json({error:'แนบคลิปหรือ PDF อย่างน้อยหนึ่งไฟล์'},400);let videoKey=null,pdfKey=null;if(video?.size){if(!['video/mp4','video/webm'].includes(video.type)||video.size>200*1024*1024)return json({error:'คลิปต้องเป็น MP4 หรือ WEBM ไม่เกิน 200 MB'},400);videoKey=`seller-course-${course.id}-video-${crypto.randomUUID()}.${extension(video.name,video.type)}`;await ctx.env.FILES.put(videoKey,await video.arrayBuffer(),{httpMetadata:{contentType:video.type}})}if(pdf?.size){if(pdf.type!=='application/pdf'||pdf.size>100*1024*1024){if(videoKey)await ctx.env.FILES.delete(videoKey);return json({error:'เอกสารต้องเป็น PDF ไม่เกิน 100 MB'},400)}pdfKey=`seller-course-${course.id}-pdf-${crypto.randomUUID()}.pdf`;await ctx.env.FILES.put(pdfKey,await pdf.arrayBuffer(),{httpMetadata:{contentType:'application/pdf'}})}const max=await ctx.env.DB.prepare('SELECT COALESCE(MAX(sort_order),0) n FROM course_lessons WHERE course_id=?').bind(course.id).first();const result=await ctx.env.DB.prepare('INSERT INTO course_lessons(course_id,title,description,sort_order,video_key,pdf_key,video_mime,pdf_mime,duration_seconds) VALUES(?,?,?,?,?,?,?,?,?)').bind(course.id,title,String(form.get('description')||''),Number(max.n)+10,videoKey,pdfKey,video?.type||null,pdfKey?'application/pdf':null,Math.max(0,Number(form.get('duration_seconds'))||0)).run();await ctx.env.DB.prepare("UPDATE courses SET total_minutes=(SELECT CAST(COALESCE(SUM(duration_seconds),0)/60 AS INTEGER) FROM course_lessons WHERE course_id=?),review_status='draft',active=0,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(course.id,course.id).run();return json({ok:true,id:result.meta.last_row_id,message:'เพิ่มบทเรียนแล้ว'},201)}
