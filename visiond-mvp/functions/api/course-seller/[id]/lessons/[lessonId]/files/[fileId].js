import {json,requireUser} from '../../../../../../_lib.js';import {ensureDatabase} from '../../../../../../_schema.js';
export async function onRequestDelete(ctx){await ensureDatabase(ctx.env);const auth=await requireUser(ctx);if(auth.error)return auth.error;const row=await ctx.env.DB.prepare("SELECT f.id,f.object_key,c.edit_expires_at FROM course_lesson_files f JOIN course_lessons l ON l.id=f.lesson_id JOIN courses c ON c.id=l.course_id JOIN products p ON p.id=c.product_id WHERE f.id=? AND l.id=? AND c.id=? AND c.owner_user_id=? AND c.course_origin='seller_rights' AND p.deleted_at IS NULL").bind(ctx.params.fileId,ctx.params.lessonId,ctx.params.id,auth.user.id).first();if(!row)return json({error:'ไม่พบไฟล์ประกอบ'},404);if(row.edit_expires_at&&Date.parse(row.edit_expires_at)<=Date.now())return json({error:'หมดระยะเวลาแก้ไขแล้ว'},403);await ctx.env.FILES.delete(row.object_key);await ctx.env.DB.prepare('DELETE FROM course_lesson_files WHERE id=?').bind(row.id).run();return json({ok:true,message:'ลบไฟล์ประกอบแล้ว'})}
