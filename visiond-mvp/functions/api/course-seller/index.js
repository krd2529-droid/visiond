import {json,requireUser} from '../../_lib.js';
import {ensureDatabase} from '../../_schema.js';
const imageTypes=['image/jpeg','image/png','image/webp'],ext=(name,type)=>String(name||'').split('.').pop()?.toLowerCase()||({'image/jpeg':'jpg','image/png':'png','image/webp':'webp'}[type]||'bin'),slugify=v=>String(v||'').trim().toLowerCase().replace(/[^a-z0-9ก-๙]+/g,'-').replace(/^-|-$/g,'').slice(0,70)||`course-${Date.now()}`;
async function putImage(env,file){if(!(file instanceof File)||!file.size)return null;if(!imageTypes.includes(file.type)||file.size>8*1024*1024)throw new Error('IMAGE_INVALID');const key=`user-course-cover-${crypto.randomUUID()}.${ext(file.name,file.type)}`;await env.FILES.put(key,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});return key}
function episodePlan(form){
  const expected=Number(form.get('expected_episodes'));
  if(!Number.isInteger(expected)||expected<1||expected>200)throw new Error('EP_COUNT');
  const raw=String(form.get('episodes_json')||'').trim();
  if(!raw)return Array.from({length:expected},(_,index)=>({title:`EP.${index+1}`,description:'',duration_seconds:0}));
  let items;try{items=JSON.parse(raw)}catch{throw new Error('EP_JSON')}
  if(!Array.isArray(items)||items.length!==expected)throw new Error('EP_LENGTH');
  return items.map((item,index)=>{
    const title=String(item?.title||'').trim().slice(0,200),description=String(item?.description||'').trim().slice(0,5000),duration=Number(item?.duration_seconds||0);
    if(!title)throw new Error(`EP_TITLE_${index+1}`);
    if(!Number.isInteger(duration)||duration<0||duration>86400)throw new Error(`EP_DURATION_${index+1}`);
    return {title,description,duration_seconds:duration};
  });
}
export async function onRequestGet(ctx){await ensureDatabase(ctx.env);const auth=await requireUser(ctx);if(auth.error)return auth.error;const profile=await ctx.env.DB.prepare(`SELECT seller_bank_name bank_name,seller_account_name account_name,seller_account_number account_number,seller_payment_status status,CASE WHEN LENGTH(seller_slip_api_key)>=20 THEN 1 ELSE 0 END slip_api_configured FROM users WHERE id=?`).bind(auth.user.id).first();const credits=await ctx.env.DB.prepare(`SELECT cr.id credit_id,cr.active,cr.used_at,cr.granted_at,p.title license_title FROM course_right_credits cr JOIN products p ON p.id=cr.product_id WHERE cr.user_id=? ORDER BY cr.id DESC`).bind(auth.user.id).all();const licenses=(credits.results||[]).map(x=>({...x,entitlement_id:`credit-${x.credit_id}`,available:Boolean(x.active),credit:1}));const courses=await ctx.env.DB.prepare(`SELECT c.id,c.license_entitlement_id,p.slug,p.title,p.short_description,p.description,p.price,p.cover_url,p.status,c.teacher_name,c.contact_info,c.platform_tags,c.learner_level,c.expected_episodes,c.review_status,c.review_note,c.edit_expires_at,COALESCE((SELECT COUNT(*) FROM course_lessons l WHERE l.course_id=c.id),0) planned_lesson_count,COALESCE((SELECT COUNT(*) FROM course_lessons l WHERE l.course_id=c.id AND TRIM(COALESCE(l.title,''))<>'' AND (l.video_key IS NOT NULL OR l.pdf_key IS NOT NULL OR EXISTS(SELECT 1 FROM course_lesson_files f WHERE f.lesson_id=l.id))),0) lesson_count FROM courses c JOIN products p ON p.id=c.product_id WHERE c.owner_user_id=? AND c.course_origin='seller_rights' AND p.deleted_at IS NULL ORDER BY c.id DESC`).bind(auth.user.id).all();const sales=await ctx.env.DB.prepare(`SELECT o.id,o.order_no,o.total,o.updated_at paid_at,p.title course_title,u.name buyer_name FROM orders o JOIN users u ON u.id=o.user_id JOIN products p ON p.id=(SELECT product_id FROM order_items WHERE order_id=o.id LIMIT 1) WHERE o.course_owner_user_id=? AND o.status='paid' ORDER BY o.updated_at DESC LIMIT 200`).bind(auth.user.id).all();const issues=await ctx.env.DB.prepare(`SELECT o.id,o.order_no,o.total,o.slip_verification_code,o.updated_at,p.title course_title,u.name buyer_name FROM orders o JOIN users u ON u.id=o.user_id JOIN products p ON p.id=(SELECT product_id FROM order_items WHERE order_id=o.id LIMIT 1) WHERE o.course_owner_user_id=? AND o.status='pending_review' AND o.slip_key IS NOT NULL AND o.slip_verification_status='manual' ORDER BY o.updated_at DESC LIMIT 100`).bind(auth.user.id).all();const items=courses.results||[],saleItems=sales.results||[],issueItems=issues.results||[],freeDrafts=items.filter(x=>x.license_entitlement_id===null);return json({payment_profile:profile,licenses,courses:items,sales:saleItems,slip_issues:issueItems,totals:{amount:saleItems.reduce((sum,x)=>sum+Number(x.total||0),0),orders:saleItems.length},course_draft_limit:3,course_draft_count:freeDrafts.length,credit_balance:licenses.filter(x=>x.available).length},200,{'cache-control':'no-store'})}
export async function onRequestPost(ctx){await ensureDatabase(ctx.env);const auth=await requireUser(ctx);if(auth.error)return auth.error;const count=await ctx.env.DB.prepare(`SELECT COUNT(*) total FROM courses c JOIN products p ON p.id=c.product_id WHERE c.owner_user_id=? AND c.course_origin='seller_rights' AND c.license_entitlement_id IS NULL AND p.deleted_at IS NULL`).bind(auth.user.id).first();if(Number(count?.total)>=3)return json({error:'สร้างคอร์สร่างฟรีครบ 3 คอร์สแล้ว กรุณาเลือกคอร์สไปผูกตะกร้าก่อน'},409);const form=await ctx.request.formData(),title=String(form.get('title')||'').trim(),teacher=String(form.get('teacher_name')||'').trim(),description=String(form.get('description')||'').trim(),draftPrice=Math.round(Number(form.get('price_baht'))*100);if(!title||!teacher||!description)return json({error:'กรุณากรอกชื่อคอร์ส ชื่อผู้สอน และรายละเอียดให้ครบ'},400);if(!Number.isFinite(draftPrice)||draftPrice<0)return json({error:'กรุณาระบุราคาขายตั้งแต่ 0 บาทขึ้นไป'},400);let plan;try{plan=episodePlan(form)}catch(error){const code=String(error.message||'');return json({error:code==='EP_COUNT'?'จำนวน EP ต้องเป็นเลขจำนวนเต็ม 1–200':code==='EP_JSON'||code==='EP_LENGTH'?'ข้อมูล EP ไม่ตรงกับจำนวนที่ระบุ':code.startsWith('EP_TITLE_')?`กรุณาใส่ชื่อ ${code.replace('EP_TITLE_','EP.')}`:'ระยะเวลา EP ต้องเป็นวินาทีจำนวนเต็ม 0–86,400'},400)}const episodes=plan.length;let coverKey;try{coverKey=await putImage(ctx.env,form.get('cover'))}catch{return json({error:'รูปปกต้องเป็น JPG, PNG หรือ WEBP ไม่เกิน 8 MB'},400)}if(!coverKey)return json({error:'กรุณาแนบรูปปกคอร์ส'},400);const allowed=new Set(['Shopee','TikTok','Lazada','Kalodata','YouTube','Gemini','Grok','ChatGPT','Facebook','Canva','อื่น ๆ']),tags=[...new Set(form.getAll('platform_tags').map(String).filter(x=>allowed.has(x)))],other=String(form.get('platform_other')||'').trim().slice(0,60);if(tags.includes('อื่น ๆ')&&other)tags.push(other);let slug=slugify(title),n=1;while(await ctx.env.DB.prepare('SELECT id FROM products WHERE slug=?').bind(slug).first())slug=`${slugify(title)}-${++n}`;const coverUrl='/api/media/'+coverKey,short=String(form.get('short_description')||'').slice(0,1000),contact=String(form.get('contact_info')||'').trim().slice(0,200),level=['beginner','intermediate','advanced','all'].includes(String(form.get('learner_level')))?String(form.get('learner_level')):'beginner',planJson=JSON.stringify(plan);try{const statements=[ctx.env.DB.prepare(`INSERT INTO products(slug,title,short_description,description,price,cover_url,preview_urls,category,file_type,pages,status,source,product_kind) SELECT ?,?,?,?,?,?,?,'online-course','คอร์สออนไลน์',?,'draft','user-course-draft','course' WHERE (SELECT COUNT(*) FROM courses c JOIN products p ON p.id=c.product_id WHERE c.owner_user_id=? AND c.course_origin='seller_rights' AND c.license_entitlement_id IS NULL AND p.deleted_at IS NULL)<3`).bind(slug,title,short,description,draftPrice,coverUrl,JSON.stringify([coverUrl]),episodes,auth.user.id),ctx.env.DB.prepare(`INSERT INTO courses(product_id,subtitle,teacher_name,active,course_type,license_edit_days,owner_user_id,license_entitlement_id,edit_expires_at,contact_info,platform_tags,learner_level,expected_episodes,review_status,course_origin) SELECT id,?,?,0,'online_course',0,?,NULL,NULL,?,?,?,?,'draft','seller_rights' FROM products WHERE slug=?`).bind(short,teacher,auth.user.id,contact,JSON.stringify(tags),level,episodes,slug),ctx.env.DB.prepare(`INSERT INTO course_lessons(course_id,title,description,sort_order,duration_seconds) SELECT c.id,json_extract(ep.value,'$.title'),json_extract(ep.value,'$.description'),(CAST(ep.key AS INTEGER)+1)*10,CAST(json_extract(ep.value,'$.duration_seconds') AS INTEGER) FROM courses c JOIN products p ON p.id=c.product_id CROSS JOIN json_each(?) ep WHERE p.slug=? AND c.owner_user_id=?`).bind(planJson,slug,auth.user.id)];await ctx.env.DB.batch(statements);const course=await ctx.env.DB.prepare('SELECT c.id FROM courses c JOIN products p ON p.id=c.product_id WHERE p.slug=? AND c.owner_user_id=?').bind(slug,auth.user.id).first();if(!course)throw new Error('COURSE_MISSING');return json({ok:true,id:course.id,slug,episode_count:episodes,message:`สร้างคอร์สร่างและเตรียม ${episodes} EP แล้ว ยังไม่หักเครดิต`},201)}catch(error){await ctx.env.FILES.delete(coverKey).catch(()=>{});return json({error:'สร้างคอร์สร่างไม่สำเร็จหรือสร้างคอร์สร่างฟรีครบ 3 คอร์สแล้ว'},409)}}
