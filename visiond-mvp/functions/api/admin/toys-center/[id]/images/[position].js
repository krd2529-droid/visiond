import {requireAdmin} from '../../../../../_lib.js';
const missing=()=>new Response('Not found',{status:404,headers:{'cache-control':'no-store','x-content-type-options':'nosniff'}});
export async function onRequestGet(ctx){const auth=await requireAdmin(ctx);if(auth.error)return auth.error;const position=Number(ctx.params.position),version=Number(new URL(ctx.request.url).searchParams.get('v'));if(!Number.isInteger(position)||position<0||position>9||!Number.isSafeInteger(version)||version<1)return missing();const row=await ctx.env.DB.prepare('SELECT image_key FROM toys_center_product_images WHERE id=? AND product_id=? AND position=?').bind(version,ctx.params.id,position).first();if(!row?.image_key)return missing();const object=await ctx.env.FILES.get(row.image_key);if(!object)return missing();const type=String(object.httpMetadata?.contentType||'');if(!type.startsWith('image/'))return missing();const responseHeaders=new Headers();object.writeHttpMetadata(responseHeaders);responseHeaders.set('cache-control','no-store, private');responseHeaders.set('etag',object.httpEtag);responseHeaders.set('x-content-type-options','nosniff');return new Response(object.body,{headers:responseHeaders})}
