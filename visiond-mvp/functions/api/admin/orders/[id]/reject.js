import {json,requireAdmin} from '../../../../_lib.js';export async function onRequestPost(ctx){const a=await requireAdmin(ctx);if(a.error)return a.error;const b=await ctx.request.json().catch(()=>({}));await ctx.env.DB.prepare("UPDATE orders SET status='rejected',admin_note=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(b.note||'หลักฐานไม่ถูกต้อง',ctx.params.id).run();return json({ok:true})}
