import {json,requireAdmin,sha256} from '../../_lib.js';
import {decryptChannelValue,encryptChannelValue} from '../../_channel_crypto.js';
const headers={'cache-control':'private, no-store'},clean=(v,n=500)=>String(v||'').replace(/[\u0000-\u001f\u007f]/g,' ').trim().replace(/\s+/g,' ').slice(0,n);
export async function onRequestPost(ctx){const auth=await requireAdmin(ctx);if(auth.error)return auth.error;const body=await ctx.request.json().catch(()=>({})),after=clean(body.after,300),row=await ctx.env.DB.prepare("SELECT shop_id,external_account_id,token_ciphertext FROM v12_channel_credentials WHERE provider='facebook' AND verified_at IS NOT NULL").first();if(!row?.token_ciphertext)return json({error:'Facebook Page ยังไม่พร้อม'},409,headers);try{const token=await decryptChannelValue(ctx.env,row.token_ciphertext),params=new URLSearchParams({fields:'id,participants,messages.limit(100){id,message,from,created_time,attachments}',limit:'25',access_token:token});if(after)params.set('after',after);const response=await fetch(`https://graph.facebook.com/v23.0/${encodeURIComponent(row.external_account_id)}/conversations?${params}`),payload=await response.json();if(!response.ok)throw new Error(`META_HISTORY_${payload?.error?.code||response.status}`);let conversations=0,messages=0;for(const item of payload.data||[]){const participant=(item.participants?.data||[]).find(x=>String(x.id)!==String(row.external_account_id));if(!participant?.id)continue;const conversationId=`facebook_${(await sha256(`${row.shop_id}:${participant.id}`)).slice(0,48)}`,participantHash=await sha256(`facebook:${participant.id}`),target=await encryptChannelValue(ctx.env,participant.id),name=clean(participant.name||'ลูกค้าจาก Facebook',120);await ctx.env.DB.batch([ctx.env.DB.prepare(`INSERT INTO veasy_conversations(shop_id,id,platform,participant_hash,display_name,updated_at) VALUES(?,?,'facebook',?,?,CURRENT_TIMESTAMP) ON CONFLICT(shop_id,id) DO UPDATE SET display_name=excluded.display_name`).bind(row.shop_id,conversationId,participantHash,name),ctx.env.DB.prepare(`INSERT INTO veasy_conversation_controls(shop_id,conversation_id,mode,provider,target_ciphertext) VALUES(?,?,'human','facebook',?) ON CONFLICT(shop_id,conversation_id) DO UPDATE SET target_ciphertext=excluded.target_ciphertext`).bind(row.shop_id,conversationId,target)]);conversations++;for(const message of item.messages?.data||[]){const attachment=message.attachments?.data?.find(x=>x.image_data||x.file_url),mediaUrl=clean(attachment?.image_data?.url||attachment?.file_url||'',1000),content=clean(message.message,4900)||(mediaUrl?'[รูปภาพ]':'[ข้อความ Facebook]'),role=String(message.from?.id)===String(row.external_account_id)?'assistant':'user',result=await ctx.env.DB.prepare(`INSERT OR IGNORE INTO veasy_chat_messages(id,shop_id,conversation_id,platform_message_id,role,content,message_type,media_url,media_mime,created_at) VALUES(?,?,?,?,?,?,?,?,?,?)`).bind(crypto.randomUUID(),row.shop_id,conversationId,clean(message.id,200),role,content,mediaUrl?'image':'text',mediaUrl,mediaUrl?'image/jpeg':'',message.created_time||new Date().toISOString()).run();messages+=Number(result.meta?.changes||0)}}return json({ok:true,conversations,messages,next_cursor:payload.paging?.cursors?.after||'',has_more:Boolean(payload.paging?.next)},200,headers)}catch(error){console.error('V12_FACEBOOK_HISTORY_FAILED',{error:clean(error?.message,80)});return json({error:'นำเข้าประวัติ Facebook ไม่สำเร็จ',code:clean(error?.message,80)},502,headers)}}
