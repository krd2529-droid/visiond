import {json,requireAdmin} from '../../../_lib.js';
const clean=(value,max)=>String(value??'').trim().slice(0,max);
export async function onRequestPost(ctx){const auth=await requireAdmin(ctx);if(auth.error)return auth.error;const body=await ctx.request.json().catch(()=>({})),title=clean(body.title,160),content=clean(body.content,30000),instructions=clean(body.instructions,2000);if(!title||!content)return json({error:'ไม่พบเนื้อหาโน้ตต้นฉบับ'},400);const key=clean(ctx.env.GEMINI_API_KEY||ctx.env.GEMINI_API_KEY_2,500);if(!key)return json({error:'ยังไม่ได้เชื่อม Gemini API สำหรับเติมเนื้อหา'},503);const prompt=`คุณเป็นบรรณาธิการเนื้อหาสำหรับงานนำเสนอภาษาไทย จงเติมและเรียบเรียงโน้ตที่อาจยังไม่สมบูรณ์ให้เป็นต้นฉบับพร้อมทำ PowerPoint โดยรักษาข้อความและป้าย [รูป N] ไว้ ห้ามสร้างตัวเลข ชื่อบุคคล ผลวิจัย เหตุการณ์ หรือข้อเท็จจริงเฉพาะที่ต้นฉบับไม่ได้ให้มา หากจำเป็นต้องเสนอแนวคิดใหม่ ให้ขึ้นต้นย่อหน้านั้นด้วย [ข้อเสนอเพิ่มเติม] หากข้อมูลสำคัญยังขาด ให้เขียน [ข้อมูลที่ต้องเติม: ...] ตอบเป็นข้อความล้วน ไม่ใช้ JSON และไม่พูดถึงกระบวนการของ AI\nหัวข้อ: ${title}\nความต้องการของผู้ใช้: ${instructions||'เรียบเรียงให้ครบและนำเสนอได้'}\nโน้ตต้นฉบับ:\n${content}`;const response=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent',{method:'POST',headers:{'content-type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{temperature:.25,maxOutputTokens:7000}})}),data=await response.json().catch(()=>({}));if(!response.ok)return json({error:response.status===429?'โควตา AI เต็ม กรุณาลองใหม่':'AI เติมเนื้อหาไม่สำเร็จ'},502);const expanded=clean((data.candidates?.[0]?.content?.parts||[]).map(part=>part.text||'').join(''),30000);if(!expanded)return json({error:'AI ไม่ได้ส่งเนื้อหากลับมา'},502);return json({ok:true,expanded_content:expanded},200,{'cache-control':'private, no-store'})}
