import {json,requireAdmin} from '../../_lib.js';import {ensureDatabase} from '../../_schema.js';import {ensureVision7Schema} from '../../_vision7_schema.js';
const date=v=>/^\d{4}-\d{2}-\d{2}$/.test(String(v||''))?String(v):null,num=v=>Number(v)||0;
export async function onRequestGet(ctx){await ensureDatabase(ctx.env);await ensureVision7Schema(ctx.env);const a=await requireAdmin(ctx);if(a.error)return a.error;const u=new URL(ctx.request.url),to=date(u.searchParams.get('to'))||new Date().toISOString().slice(0,10),from=date(u.searchParams.get('from'))||new Date(Date.now()-29*86400000).toISOString().slice(0,10);const rows=await ctx.env.DB.prepare(`SELECT c.platform,c.campaign,c.adset,c.creative,SUM(c.cost) spend,COUNT(DISTINCT o.id) orders,COALESCE(SUM(DISTINCT CASE WHEN o.status='paid' THEN o.total ELSE 0 END),0) revenue FROM ad_campaign_costs c LEFT JOIN customer_events e ON e.campaign=c.campaign AND (e.content=c.creative OR c.creative='') AND e.event_type='purchase' LEFT JOIN orders o ON o.id=e.order_id AND o.status='paid' WHERE c.spend_date BETWEEN ? AND ? GROUP BY c.platform,c.campaign,c.adset,c.creative ORDER BY spend DESC`).bind(from,to).all(),items=(rows.results||[]).map(r=>{const spend=num(r.spend),revenue=num(r.revenue),roas=spend?revenue/spend:null;let decision='observe',reason='ข้อมูลยังไม่พอ ควรเก็บข้อมูลต่อ';if(spend>0&&revenue===0){decision='stop_or_adjust';reason='มีค่าใช้จ่ายแต่ยังไม่พบยอดขายที่ระบุแหล่งที่มา'}else if(roas>=2){decision='continue';reason='ROAS ตั้งแต่ 2 เท่าขึ้นไป'}else if(roas!==null&&roas<1){decision='adjust';reason='รายได้ต่ำกว่าค่าโฆษณา'}return {...r,spend,revenue,orders:num(r.orders),roas,profit:revenue-spend,decision,reason}});const summary=items.reduce((s,x)=>({spend:s.spend+x.spend,revenue:s.revenue+x.revenue,orders:s.orders+x.orders}),{spend:0,revenue:0,orders:0});summary.profit=summary.revenue-summary.spend;summary.roas=summary.spend?summary.revenue/summary.spend:null;const page=await ctx.env.DB.prepare(`SELECT COUNT(*) conversations,SUM(status='human_required') handoff_waiting FROM elon_page_conversations WHERE date(created_at,'+7 hours') BETWEEN ? AND ?`).bind(from,to).first();return json({from,to,summary,page_sales:page,items,note:'คำแนะนำเป็นข้อมูลช่วยตัดสินใจ ระบบยังไม่แก้ไขโฆษณาอัตโนมัติ'});}
