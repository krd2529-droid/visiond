import {json,requireUser} from '../../../_lib.js';
export async function onRequestGet(ctx){const auth=await requireUser(ctx);if(auth.error)return json({allowed:false,message:'กรุณาเข้าสู่ระบบ'},401);const entitlement=await ctx.env.DB.prepare('SELECT id FROM entitlements WHERE user_id=? AND product_id=? AND active=1').bind(auth.user.id,ctx.params.id).first();if(!entitlement)return json({allowed:false,message:'ยังไม่มีออเดอร์ที่ได้รับอนุมัติสำหรับสินค้านี้'},403);const own=await ctx.env.DB.prepare('SELECT pf.id,pf.label,pf.version,pf.file_size,pf.mime_type,0 sort_order FROM product_files pf WHERE pf.product_id=? ORDER BY pf.id').bind(ctx.params.id).all(),bundled=await ctx.env.DB.prepare("SELECT pf.id,(p.title||' · '||pf.label) label,pf.version,pf.file_size,pf.mime_type,b.sort_order FROM product_bundle_items b JOIN products p ON p.id=b.source_product_id JOIN product_files pf ON pf.product_id=p.id WHERE b.bundle_product_id=? ORDER BY b.sort_order,pf.id").bind(ctx.params.id).all();return json({allowed:true,files:[...(own.results||[]),...(bundled.results||[])]})}
