import {json,requireUser} from '../../../_lib.js';export async function onRequestGet(ctx){const a=await requireUser(ctx);if(a.error)return json({allowed:false,message:'กรุณาเข้าสู่ระบบ'},401);const e=await ctx.env.DB.prepare('SELECT id FROM entitlements WHERE user_id=? AND product_id=? AND active=1').bind(a.user.id,ctx.params.id).first();if(!e)return json({allowed:false,message:'ยังไม่มีออเดอร์ที่ได้รับอนุมัติสำหรับสินค้านี้'},403);const {results}=await ctx.env.DB.prepare('SELECT id,label,version,file_size FROM product_files WHERE product_id=?').bind(ctx.params.id).all();return json({allowed:true,files:results})}
