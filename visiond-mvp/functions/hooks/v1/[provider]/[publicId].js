import {json} from '../../../_lib.js';import {decryptChannelValue,lineSignatureValid} from '../../../_channel_crypto.js';import {cleanWebhookProvider,ensureWebhookHubSchema} from '../../../_webhook_hub.js';
const out=(data,status)=>json(data,status,{'cache-control':'no-store'});async function find(ctx){const provider=cleanWebhookProvider(ctx.params.provider),publicId=String(ctx.params.publicId||'');if(!provider||!/^wh_[a-f0-9]{48}$/.test(publicId))return null;await ensureWebhookHubSchema(ctx.env);return ctx.env.DB.prepare('SELECT id,status,adapter_state FROM veasy_webhook_endpoints WHERE provider=? AND public_id=?').bind(provider,publicId).first()}
export async function onRequestPost(ctx){const x=await find(ctx);if(!x||x.status==='revoked')return out({error:'ไม่พบ Webhook'},404);if(x.status==='paused')return out({error:'Webhook ถูกพักไว้',code:'WEBHOOK_PAUSED'},503);if(x.status!=='active'||x.adapter_state!=='ready')return out({error:'ลิงก์ถูกสร้างแล้ว แต่ Provider Adapter ยังไม่พร้อมรับข้อความ',code:'WEBHOOK_PROVIDER_NOT_CONNECTED'},503);if(ctx.params.provider!=='line')return out({error:'Provider Adapter ยังไม่ถูกติดตั้ง',code:'WEBHOOK_ADAPTER_REQUIRED'},503);const raw=await ctx.request.text(),signature=ctx.request.headers.get('x-line-signature'),credential=await ctx.env.DB.prepare("SELECT secret_ciphertext FROM veasy_channel_credentials WHERE shop_id=(SELECT shop_id FROM veasy_webhook_endpoints WHERE id=?) AND provider='line'").bind(x.id).first();if(!credential)return out({error:'LINE ยังไม่พร้อม'},503);let valid=false;try{valid=await lineSignatureValid(await decryptChannelValue(ctx.env,credential.secret_ciphertext),raw,signature)}catch{}const requestId=crypto.randomUUID();if(!valid){await ctx.env.DB.batch([ctx.env.DB.prepare("UPDATE veasy_webhook_endpoints SET failed_count=failed_count+1,last_event_at=CURRENT_TIMESTAMP WHERE id=?").bind(x.id),ctx.env.DB.prepare("INSERT INTO veasy_webhook_events(id,endpoint_id,request_id,signature_status,processing_status,error_code) VALUES(?,?,?,'invalid','rejected','LINE_SIGNATURE_INVALID')").bind(crypto.randomUUID(),x.id,requestId)]);return out({error:'ลายเซ็น LINE ไม่ถูกต้อง'},401)}let payload={};try{payload=JSON.parse(raw)}catch{return out({error:'JSON ไม่ถูกต้อง'},400)}const events=Array.isArray(payload.events)?payload.events:[],statements=[ctx.env.DB.prepare("UPDATE veasy_webhook_endpoints SET received_count=received_count+?,last_event_at=CURRENT_TIMESTAMP WHERE id=?").bind(events.length,x.id)];for(const e of events.slice(0,100))statements.push(ctx.env.DB.prepare("INSERT OR IGNORE INTO veasy_webhook_events(id,endpoint_id,request_id,external_event_id,signature_status,processing_status,event_type) VALUES(?,?,?,?, 'valid','received',?)").bind(crypto.randomUUID(),x.id,`${requestId}:${crypto.randomUUID()}`,String(e.webhookEventId||''),String(e.type||'')));await ctx.env.DB.batch(statements);return out({ok:true},200)}
export async function onRequestGet(){return out({error:'Method not allowed'},405)}
