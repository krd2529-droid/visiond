# Active patch: Storefront login parity and boss mobile preview lifecycle

- Event: PATCH_BLOCKED
- Outcome: Login and registration share the existing button treatment, with separate login-link and signup-link classes and shared CSS selectors; login precedes registration. Mobile preview exists only for a server-confirmed boss on eligible desktop storefronts.
- Phase: code commit 49271841 pushed to origin/main and deployed v0.20.57; production guest UI verified; actual authenticated boss flow awaits user session.
- Authorization: /api/auth/me supplies session expiry from the same indexed session/user query, opt-in only; response is private, no-store. Shared auth-session uses one in-flight request and 30-second memory TTL, never trusts localStorage roles, and performs no background polling.
- Lifecycle: pending/error/non-boss responses have zero preview DOM. Exact server expiry removes launcher and overlay, long timers rearm safely. Logout immediately revokes and latches across tabs and pending requests until fresh authentication; hidden tabs suppress preview and return uses valid memory cache.
- Files: shared-nav, auth-session, boss-mobile-preview, nav-account, all existing logout entrypoints, /auth/me and optional currentUser expiry projection; dependency cache keys propagated through public importers; version v0.20.57 and focused regressions.
- Verified: test:v02057 (roles, malformed/error/pending responses, request dedup, TTL, expiry, logout races, cross-tab latch, visibility, private response, one-query indexed EXPLAIN); v02054 catalog, v02055 login reliability, v02056 guides, admin-product-d1-efficiency, syntax and visible-version parity passed. Historical tests updated only for current asset/version keys.
- Prior-session Mark evidence (reported in deployed record; artifact is unavailable in this shared workspace): .tmp-codex/mark-nav-qa/race.mjs now bossRecreated:false and calls:1; browser guest/user/admin preview DOM0, boss opens iframe, six-second expiry removes all preview DOM, desktop1280 buttons39px and mobile390 buttons46px with no overflow.
- Deferred separate gate: actual TikTok reviewer-account validation remains subject to its existing D1-reset gate.
- Production verified by Jarvis: six navigation/CSS assets HTTP200 match tested local source; real browser desktop1280 CTAs white39px, mobile390 CTAs white46px, correct order, no overflow, preview DOM0 for guest; login CTA opens /login form. Guest auth API401 private/no-store. Existing catalog load failure predates patch and remains separate. Browser console error/warn log empty during this check.
- Remaining gate: user logs into boss in the production browser, then verify actual role confirmation, preview open/close and logout removal. No boss production session available; local role fixtures do not substitute for this gate. Unrelated user files remain untracked and untouched.

- Reconciliation 2026-09-07: inspected all 23 dirty tracked files and one untracked focused test; all were superseded login-patch work. Byte-exact originals, SHA-256 manifest and binary tracked patch are saved locally at `.tmp-codex/reconcile-20260907-elon/`. Local main fast-forwarded from 57453143 to 7bfd3c66; tracked/index diff was empty afterward. Existing unrelated untracked manuals, images and output were untouched.
- Fresh local verification 2026-09-07: test:v02057 passes on 14 storefronts and auth roles/errors/dedup/TTL/expiry/logout races/visibility/private response; EXPLAIN confirms indexed session/user lookup. v02054, v02055, v02056 and admin-product-d1-efficiency pass; the v02055 simulated D1 quota log is expected. Four navigation/auth JavaScript syntax checks and git diff --check pass.
- Fresh production verification 2026-09-07 (Jarvis): shared-nav.js, auth-session.js, boss-mobile-preview.js and nav-account.js at v=02057 return HTTP200 and match origin/main after line-ending normalization. Real guest browser shows WEB v0.20.57, ordered login/register CTAs and successful navigation to /login. Actual boss login, preview and logout verification still awaits an authenticated user session.
